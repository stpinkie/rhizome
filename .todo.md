# Phase 1 Leftovers

These items are not blockers, but they should be cleaned up before Phase 1 is considered fully polished.

## Rebrand / Docs
- [ ] Replace remaining `picoclaw.io` links with a new docs domain or GitHub repo links.
  - `README.md` (badges, download links, official domain text)
  - `docs/project/README.*.md` (all translations)
  - `docs/guides/android-termux.md`
  - `web/backend/i18n.go` (`DocUrl` English + Chinese)
  - `web/frontend/src/components/app-header.tsx`
  - `web/frontend/src/components/channels/channel-config-page.tsx`
- [ ] Rewrite `README.md` top section: it still says "initiated by Sipeed" and "not a fork"; update to state Rhizome is a hard fork of PicoClaw.
- [ ] Verify / update `LICENSE` and `CONTRIBUTING.md` to keep PicoClaw attribution while adding Rhizome copyright.
- [ ] Replace `assets/logo.webp` and ASCII banner in `cmd/rhizome/main.go` with actual Rhizome branding (optional for now).

## Identity / Security
- [ ] Encrypt the persisted node identity (`~/.rhizome/identity/node.json`) with a local passphrase (currently stored base64-only).
- [ ] Support optional BIP39 mnemonic generation in `rhizome network onboard` instead of requiring the user to supply one.
- [ ] Decide whether to cache the BIP39 root seed (encrypted) for network auth, or require the user to re-enter the mnemonic when needed.

## Network / CLI
- [ ] `rhizome network onboard` should validate that the chosen `node_index` is not already in use on the local machine.
- [ ] `rhizome daemon` currently only starts the P2P host; it should eventually start the agent gateway as well (Phase 2+).
- [ ] Add DHT discovery as a fallback when mDNS is not sufficient.
- [ ] Add integration test that runs `rhizome daemon` and `rhizome network ping` as real processes.

## Build / Release
- [ ] Review `.goreleaser.yaml` Docker image names and Docker Hub env vars.
- [ ] Confirm `web/rhizome-launcher.png` and desktop file references are wired up in the frontend build.
- [ ] Measure binary size impact of libp2p; decide whether to add a `nonetwork` build tag for lightweight builds.
