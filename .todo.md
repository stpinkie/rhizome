# Open Items

Only genuinely open work is tracked here. Completed Phase 1 leftovers and the
v0.4 portability phases are collapsed below — see git history for details.

## Deferred to later phases

- [ ] Replace `assets/logo.webp` with actual Rhizome branding.
- [ ] Replace PicoClaw mascot slogan/shrimp emoji in translated READMEs (`docs/project/README.*.md`).
- [ ] Refresh translated README news sections (English README updated for v0.4.x/v0.5.0; translations still show the old news).
- [ ] Re-enable scheduled `nightly.yml` when release infrastructure (Docker Hub, GHCR, macOS notary) is ready.
- [ ] Re-add `docker-build.yml` if a separate manual Docker build is needed once Docker Hub credentials are in place.
- [ ] Phase 3 — qemu 32-bit mesh tests and real-device Android smoke tests (deferred; current environment lacks QEMU / Android 4.4 test hardware).
- [ ] Phase 5 — v0.4.0 release notes for translated READMEs (English README news refreshed for v0.5.0).
- [ ] Web console: no `mesh` config section exists (only `timeouts.mesh.remote_call` is exposed); consider a Mesh config section covering `enabled`, `trusted_peers`, `allow_remote_*`, `require_signed_caps`, rate limits, and ACLs.
- [ ] `pkg/rhizome/mesh/security.go`: the `replayGuard.check` doc comment (lines ~64-66) says "missing fields are the caller's policy decision; this only validates what is present," but the function rejects missing nonces (line ~77). The comment is stale — the behavior is safer than described. Worth a comment cleanup.
- [ ] `pkg/rhizome/mesh/mesh.go`: the capability receive handler (~line 152) calls `verifyCapability` for any incoming announce, including from untrusted peers. With `require_signed_caps: true`, untrusted peers sending unsigned caps are rejected, but a trusted peer with a valid signature can still store capabilities. This is by design (capabilities are metadata; `CallRemote` separately enforces trust/ACL/rate limits before any agent runs), but the trust boundary for capability *storage* is separate from the trust boundary for remote *execution*. Document this explicitly if the mesh threat model is ever written up.

## Completed (archive)

- All Phase 1 rebrand/docs, identity/security, network/CLI, build/release, hardware-compat, CI/CD, and v0.4 portability items.
- v0.5.0 sprint: `require_signed_caps` now defaults to `true`; `nonce`/`timestamp` are required on `/rhizome/agent/1.0.0` (legacy requests rejected); `stale.yml` daily cron re-enabled; README news refreshed.
