# Open Items

Only genuinely open work is tracked here. Completed sprints are collapsed into
the archive — see git history and `docs/release-notes/` for details. Roadmap
context lives in `ROADMAP.md`.

## In progress — v0.14.0 (Tracks 97–111, revised)

Theme: **Open Agent Work Market** — plus the llama.cpp module, an upstream
cherry-pick sweep, the ACP-over-HTTPS transport, and the localization tail.
Land the generic core seams the mesh-economy design requires (ACP runtime
modes + network isolation, module capability adverts, `rhizome market` CLI
stub — the stream bridge + `acp.remote` already shipped in PR #46), ship
`rhizome-market` as a first-party daemon-kind companion module with
sell-side session serving and buy-side purchasing, and bind an adopted
escrow contract — the market ships **experimental** (fixture rail default,
direct-peer discovery; curated index, DHT tier, reputation fields, and the
recorded testnet E2E move to v0.15.0). Design docs:
`docs/design/mesh-economy-module.md`, `docs/design/v0.14.0-sprint.md`
(revised mid-implementation after Tracks 98 + 106 landed).

Assumes the v0.13.0 stack (esp. Track 94 usage wire fields — the receipt's
usage shape; Track 90's shared signature verifier for the index client;
Track 88 persistent sessions; Track 95 catalog signatures; Track 96 enc2
wallet store).

Cut lines if the sprint runs long (in order): Track 110 (HTTPS) → Track
104 (bind slips with its E2E — fixture-rail-only experimental) → Track
109's GPU-backend variants (cpu-only pin) → Track 97's container runtime
(sandbox-only) → Track 111 partial → Track 108. Tracks 99, 100, 101, 102,
103 are never cut.

### Track 97 — ACP runtime modes + network-isolation knob (core)
- [x] `acp.client.runtime = exec|sandbox|container`; per-binding
  `agents.list[].acp.runtime` override; `acp.client.container =
  {engine:auto|docker|podman, image, network:none|bridge|allowlist,
  mem_mb, cpus, pull:missing|never}` — `allowlist` is schema-accepted but
  errors explicitly at spawn (egress allowlisting needs a proxy network,
  out of scope); `acp.client.sandbox = {network:inherit|none}` added for
  the sandbox net knob.
- [x] `pkg/isolation` per-invocation API (`Options` + `StartWith` +
  `RunWith`/`PrepareCommandWith`/`PreflightWith`) — process-global
  `CurrentConfig` can't express per-binding sandboxes; `Start`/`Run`
  semantics unchanged for the supervisor path (one-line delegates).
  Scratch instance dir per ACP agent: `<home>/acp-sandbox/<agent-id>`.
- [x] Net knob: bwrap `--unshare-net` (linux), sandbox-exec profile minus
  `network-outbound` (darwin — today unconditional), Windows
  deny-with-error (job objects can't scope per-process networking —
  sanctioned by the sprint doc's deny-with-error matrix); unsupported
  platform+mode → clear spawn error, never silent.
- [x] `client.go` `spawn()` dispatch: exec = today's path; container =
  `docker|podman run -i --rm` with stdio as the ACP transport (zero
  protocol change); `engine:auto` resolves docker→podman→named error.
  Named containers + `docker kill` on teardown (killing the CLI alone
  would orphan the daemon-managed container). Live docker stdio drive in
  `integration/suites/acp-container`.

### Track 98 — Module stream bridge + `acp.remote` (core)
- [x] `ModuleSpec.Protocols []string` (catalog schema v4); validate
  well-formedness + reject collisions with core-registered protocol ids.
  Also: cross-module duplicate detection while both enabled, and dynamic
  `Mux().Protocols()` collision skip at bridge start.
- [x] Daemon loopback bridge listener (`127.0.0.1:0`, not the gateway mux —
  works under `--no-gateway`): inbound `SetStreamHandler` per declared
  protocol → splice to module; outbound `{token, peer, protocol}` hello →
  `host.NewStream` → splice back. Inbound additionally gated on
  `mesh.trusted_peers` (reset before handshake); hello reader preserves
  coalesced payload bytes via buffered-conn splice.
- [x] Per-module token minted at install (`<module_dir>/bridge-token`,
  0600); `RHIZOME_BRIDGE_ADDR`/`RHIZOME_BRIDGE_TOKEN`/`RHIZOME_MODULE_DIR`
  env injection for protocol-declaring modules; token→module→protocol
  allowlist enforced.
- [x] `agents.list[].acp.remote = "<peer-multiaddr>"` — ClientManager
  `RemoteDialer` seam (wired from the mesh host in gateway.go) dials
  `/rhizome/acp/1.0.0` to trusted peers (`mesh.IsTrusted` gate);
  `acp.server.remote = true` serves ACP over inbound streams via
  `RemoteMux` (per-conn Server, shared session store + models, additive
  `bus.AddStreamDelegate`, headless stack under `--no-gateway`).
- [x] Networked tests isolated per AGENTS.md (NewIdentity, DisableMDNS,
  DisableNATPortMap, explicit BootstrapPeers).

### Track 99 — Module capability adverts + `rhizome market` CLI stub (core)
- [ ] `Capability.ModuleAdverts map[string]json.RawMessage` (emit-when-set)
  gated on `serve_enabled`; fixture test pins old-build re-marshal
  behavior (new field = whole manifest rejected, the `Role` precedent —
  unlike `Allows` map keys which survive).
- [ ] `mesh.SetModuleAdvertProvider` — daemon reads
  `<home>/modules/<id>/advert.json` (≤16 KB, JSON-validated, errors →
  drop); `Allows["market_serve"]=true` when an advert declares serving.
- [ ] `cmd/rhizome/internal/market/`: `market find|buy|session|receipt`
  thin verbs → module localhost API (addr/token from
  `<module_dir>/api.addr`/`bridge-token`); absent → "rhizome-market
  module not installed" posture.

### Track 100 — `rhizome-market` skeleton (module)
- [ ] `cmd/rhizome-market` binary: loopback HTTP API, config from
  `modules.rhizome-market.fields`/`.secrets`, `advert.json` writer;
  links `pkg/web3` (shared wallet + `web3-pending.json`), `pkg/isolation`,
  `pkg/acp`, `pkg/rhizome/stream`, `pkg/rhizome/identity`.
- [ ] Catalog entry: `kind:daemon`, `protocols:["/rhizome/acp/1.0.0"]`,
  `github-release` on `stpinkie/rhizome`; `releases:[]` until assets
  exist (first pin in Track 107 or pipeline-emitted — impl-time pick).
- [ ] `.goreleaser.yaml`: `rhizome-market` build id (linux/darwin/windows
  amd64+arm64) + separate archive — never in base archives; checksums
  cover it for pinning; `goreleaser check` green.
- [ ] Field schema: `serve_enabled`, `offers_json` (or `offers.json`
  file), `runtime`, `payout_{chain_id,address,asset}`,
  `max_concurrent_sessions`, `session_ttl`, `buy_max_cost_per_{task,day}`,
  `export_{allow_attachments,redact,require_review}`,
  `market_index_{enabled,url}`, `escrow_{chain_id,contract,token,arbiter,
  dispute_window}`; `market-audit.jsonl` under module dir.

### Track 101 — Escrow survey + `SettlementRail` + mock (module/docs)
- [ ] `docs/design/escrow-survey.md`: Kleros
  `MultipleArbitrableTokenTransaction` (ERC-792/1497, real arbitration —
  verify Sepolia/L2 deployability + fee model) vs Smart Invoice
  (designated arbiter, factory clones, battle-tested but unaudited) vs
  OZ escrows (reject: no dispute semantics) vs custom-minimal fallback.
  Criteria: Sepolia/L2 deploy, ERC-20, dispute window + arbiter
  semantics, session-id keying, eth_getLogs surface, audit status,
  license.
- [ ] `SettlementRail` iface (`Open/VerifyLock/Release/Claim/Dispute/
  Resolve`) + fixture impl that 102/103 code against; ABI bundled
  in-module; `escrow_*` fields resolved.

### Track 102 — Sell-side: session manager + receipts (module)
- [ ] Inbound `/rhizome/acp/1.0.0` → session-open `{session_id, task_hash,
  offer_id}` (pre-ACP line or `_rhizome.session_open` — impl-time) →
  verify offer enabled + `VerifyLock` on-chain **before any LLM spend**.
  Handler is transport-agnostic (`io.ReadWriteCloser`-shaped) — Track
  110's HTTPS path reuses it unchanged.
- [ ] Spawn offer's agent under `runtime` (sandbox/container;
  `permission_policy=deny` forced, no fs/terminal caps, scratch cwd,
  container egress none|allowlist); `max_concurrent_sessions`,
  `session_ttl`, per-peer rate limits.
- [ ] Metered session: duration + `_rhizome.usage` self-report (nil
  otherwise); `_rhizome.receipt` extension response signed with node key
  — `{session_id, offer_id, duration_ms, usage, result_sha256, terms,
  signature}`.

### Track 103 — Buy-side: `market buy` + export policy (module)
- [ ] Index client: `<url>/index.json` + `.sig` Ed25519 (reuse Track-90
  shared verifier); 1 h cache + stale-serve under module dir. Generic —
  works against operator-supplied URLs; the curated index is v0.15.0.
- [ ] `market find <service>`: index query when `market_index_url` set,
  else scan connected/trusted peers' `ModuleAdverts` (direct-peer
  discovery for the experimental market).
- [ ] `market buy <provider> <offer> <task> [--max-cost]` — provider =
  peer id/`/p2p/` multiaddr via bridge dial (`wss://`/`https://` in Track
  110): export policy (text only, no attachments, `pkg/redact`,
  `require_review`) → `approve`+`open` via `web3_contract_send` +
  pending-approval → session id → bridge dial → session → verify
  `_rhizome.receipt` → `release`/`dispute`; `market session|receipt <id>`.
- [ ] Buyer caps `buy_max_cost_per_{task,day}` layered on web3 policy.

### Track 104 — Escrow binding + Sepolia deploy (module) — slimmed
- [ ] Bind the 101 pick to `SettlementRail`; deploy/adopt on Sepolia +
  the L2 pick; `eth_getLogs` watches for lock/dispute events; fixture
  unit tests.
- [ ] `escrow_contract` unset → fixture rail stays the shipped default;
  set → bound-but-unverified rail (docs mark escrow experimental).
- [ ] (→ v0.16.0 Track 132; deferred via v0.15.0 mesh/swarm depth)
  Recorded testnet E2E: buy→open→session→receipt→release;
  dispute→resolve split; claim after window; tx hashes in acceptance
  notes.

### Track 105 — Index curation + DHT tier + reputation → v0.16.0 Tracks 124/125/126

### Track 106 — ACP follow-ons tail (core, flex)
- [x] `set_config_option` `category:thought_level` — fixed enum
  (off/low/medium/high/xhigh/adaptive + inherit) → per-turn
  `ThinkingLevelOverride` on `processOptions`/`turnExecution`, beating
  model-configured levels; persisted to `acp-sessions.json` and restored
  on `session/load`.
- [x] Deeper `mcpCapabilities`: session-declared `http`/`sse` MCP servers
  now connect via the shared `pkg/mcp` manager (stdio was Track 81;
  client-side forwarding honoring advertised transports shipped earlier);
  `initialize` advertises `http+sse`. Nested `acp` and non-http(s) URLs
  stay refused.
- [x] `acp.remote` serve-side spillover if it didn't fit Track 98. (It
  fit — `RemoteMux` + headless serve shipped with Track 98.)

### Track 108 — Upstream cherry-pick sweep + issue re-triage (new; first)
- [x] Upstream `main` unchanged since sync point `bbf6893c` (2026-08-19)
  — compare API `ahead_by: 0`; zero cherry-pickable commits. All activity
  is in open PRs/issues; triage recorded in
  `docs/project/upstream-issue-triage.md` (sweep 2026-09-25).
- [x] Ported upstream PR #3378 — `RefreshAccessToken` uses `cfg.Scopes`
  (was hardcoded `openid profile email`); test asserts scope on the wire.
- [x] DingTalk channel removed pending upstream SDK fix (issue #3382 —
  SDK-internal goroutine panic, unrecoverable; only candidate is an
  unverifiable beta). Config type stays registered (existing
  `channel_list.dingtalk` configs validate; stub factory fails with a
  clear error); impl + `dingtalk-stream-sdk-go` dep + UI pickers +
  `docs/channels/dingtalk/` deleted.
- [x] Adapted port of upstream PR #3371 (opencode-go provider) —
  separate PR `devin/track-108-opencode-go`. New `ProtocolFamily`
  `"opencode-go"` routes models by ID across `/responses` (new
  `pkg/providers/openai_responses`), `/messages` (`anthropic_messages`
  gained `custom_headers`/`session_header_name` options), and
  `/chat/completions` (`openai_compat`); `x-opencode-session` flows via
  `common.ApplySessionHeader` (shared with Track-59 `session_header`,
  custom_headers win); `session_key` added to `askSideQuestion` llmOpts.
- [x] Escalation decisions: #3381 (OpenAI→Responses API) deferred until
  upstream merges; #3353/#3347 already shipped; #3376 n/a (deltachat in
  static registry); #3391 out of scope (pico client app); dep bumps left
  to our dependabot.

### Track 109 — llama.cpp module (new; design doc landed in Track 95)
- [ ] `InstallSpec.BinaryPath` — no-flatten, layout-preserving
  extraction; `binaryPath` resolves `<version>/<binary_path>`; catalog
  schema v5 (v4 = Track 98 `protocols`; lowest covering version emitted).
- [ ] Field-fed asset substitution: `{gpu_backend}` (declared catalog
  fields only) into `asset_template(s)`/`download_url` before digest
  lookup; per-variant digest keys (`goos/goarch/backend`) or per-backend
  pinned releases — impl-time pick.
- [ ] `extractTarGz` refuses `TypeSymlink`/`TypeLink` explicitly (zip
  already rejects).
- [ ] Generic weights fetch: catalog `fetch`/`init_fetch` entries
  templated from fields → download once before first `start`, streamed
  sha256 verify (installRelease posture); weights outside `<version>/`;
  `module validate` enforces `model_url ⇒ model_sha256`.
- [ ] Catalog entry `kind:ondemand` on `ggml-org/llama.cpp`: field schema
  per design (`model_url`, `model_sha256`, `gpu_backend`, `gpu_layers`,
  `ctx_size`, `threads`, `port` loopback-default, `api_key` secret,
  `mlock`, `flash_attn`, `extra_args`); `args_template` +
  `health` `/health`; first pin cpu everywhere + metal/cuda where
  published (verify asset names at impl).
- [ ] Consumer story: `openai-compatible` provider profile →
  `http://127.0.0.1:{port}/v1`; market-offer synergy documented.

### Track 110 — ACP-over-HTTPS market transport (new; Stage-D item)
- [ ] Module fields: `serve_https_listen` (empty=off), `serve_https_cert`
  /`serve_https_key` (self-signed OK; fingerprint → `advert.json` TOFU),
  `serve_https_max_conns`.
- [ ] WebSocket upgrade at `/rhizome/acp` via `gorilla/websocket`
  (existing direct dep — no new deps); per-message vs stream framing is
  an impl-time pick frozen by the 107 wire spec.
- [ ] Post-upgrade: identical `_rhizome.session_open` presentation →
  offer-enabled + `VerifyLock` gate → splice onto session transport —
  no lock → no spawn, same as libp2p.
- [ ] `advert.json` gains `endpoints` + `tls_fingerprint`; buy side
  accepts `wss://`/`https://` providers (module dials directly, no
  bridge); TLS mandatory; per-IP rate limits + session caps apply
  identically.

### Track 111 — Localization tail + integration verification (new; flex)
- [ ] Sweep `docs/project/README.*.md` (9 locales) + `docs/channels/*/
  README.*.md` for `picoclaw`/`PicoClaw`/🦐/mascot-slogan remnants +
  "ultra-efficient" h1; case-insensitive grep-clean is acceptance.
- [ ] Run `scripts/integration-mesh` + `integration-swarm` (v0.13.0
  acceptance gap); record green or file failures as new items.
- [ ] Standing RSS watch: `measure-footprint.sh` re-run in release prep.

### Track 107 — Docs & release v0.14.0
- [ ] `docs/guides/market.md` (buy/sell/security posture + threat model +
  **experimental posture** + **HTTPS wire spec** for non-Rhizome
  counterparties); `docs/guides/acp.md` (runtime modes, acp.remote,
  net-isolation matrix); `docs/guides/modules.md` (bridge/adverts/token
  posture + `binary_path`/field-fed templates/weights fetch, catalog v5);
  `docs/guides/llama-cpp.md` or modules-guide section;
  `docs/operations/market-index-signing.md` moves to v0.15.0 with 105;
  escrow-survey link.
- [ ] `config.example.json`: `acp.client.runtime`/`container`,
  `agents.list[].acp.{runtime,remote}`, `acp.server.remote`,
  `modules.rhizome-market` fields (incl. `serve_https_*`),
  `modules.llama-cpp` fields (each section lands on its owning track
  branch — the strict-loader precedent).
- [ ] `AGENTS.md` + `ROADMAP.md` sync; `.todo.md` rotate (deferrals
  recorded under the v0.15.0 queue); `docs/release-notes/v0.14.0.md`;
  tag `v0.14.0` (explicit go-ahead).

### Acceptance criteria
- [ ] Build/test/lint gates green (`goolm,stdjson`; golangci-lint v2.13.2;
  `lint-docs.sh`); no new Go deps in core or the market module.
- [ ] v0.13.0 released + verified (archives, GHCR pull, `rhizome version`).
- [ ] `acp.runtime`: exec unchanged; sandbox `network=none` demonstrably
  offline; container agent runs over docker/podman stdio.
- [x] Bridge: declared-protocol stream reaches module byte-for-byte;
  outbound dial opens a peer stream; undeclared protocol + bad token
  refused; works under `--no-gateway`. (Track 98 tests, PR #46)
- [x] `acp.remote`: two trusted nodes run an ACP task; untrusted refused.
  (Track 98 tests, PR #46)
- [ ] `ModuleAdverts`: serving node's advert in signed manifest +
  `market_serve` advert; non-serving node emits v0.13.0-identical bytes.
- [ ] Market loop on the fixture rail: buy→session-open→session→receipt→
  release; dispute/resolve/claim paths exercised; bound rail covered by
  fixture/unit tests. (Recorded testnet E2E + tx hashes → v0.15.0.)
- [ ] No-work-before-lock: unverified escrow lock never spawns the agent —
  on the libp2p and HTTPS paths.
- [ ] Export policy: attachments refused, redact pass, `require_review`.
- [ ] `rhizome market` without module → clean not-installed error.
- [ ] HTTPS transport: external WS client completes session-open→prompt→
  receipt against a sandboxed seller agent; cleartext + unpaid refused.
- [ ] llama.cpp: install→digest verify→weights fetch+pin→start→`/health`
  200→chat completion via an `openai-compatible` provider profile.
- [ ] Upstream sweep: triage table committed; picked fixes green; triage
  doc records the new sync ref.
- [ ] Localization: translated docs grep-clean for `picoclaw`/`PicoClaw`/
  🦐 remnants.
- [ ] `integration-mesh`/`integration-swarm` green.

## Open items carried

- [ ] Replace `assets/logo.webp` with actual Rhizome branding. **Blocked**:
  needs a real logo asset — no art pipeline.
- [ ] Re-enable `nightly.yml` cron + re-add `docker-build.yml` —
  **blocked** on release secrets (Docker Hub, `MACOS_SIGN_*`/
  `MACOS_NOTARY_*`).
- [ ] Phase 3 hardware: qemu 32-bit mesh tests + real-device Android smoke
  tests (deferred; no QEMU / Android test hardware; `ANDROID_API` floor is
  now 21).
- [ ] Standing watch — runtime RSS creep: re-run `measure-footprint.sh`
  each release or after large dep/runtime changes.
- [ ] Standing watch — `test-parallel` advisory CI job / `flake-hunt`: a
  recurring failure is the signal to revisit heartbeat cadence or `-p`
  throttling.

## Planned — v0.15.0

v0.15.0 is **Mesh & Swarm Depth** — the arc considered and dropped at the
v0.14.0 sprint revision, now its own sprint. Design record:
`docs/design/v0.15.0-sprint.md` (ordering, per-track architecture, risk
register). Track numbers are monotonic; v0.14.0 consumed 97–111, so this
sprint is 112–122. One PR per track, in this order; tracks that slip in
v0.14.0 prepend this queue. Cut lines (in order): Track 120 → 119 web half
→ 117 `doctor` verb → 114 per-peer ACL cap → 121 → 118 trace verbs → 116
offer persistence. Never cut: 112, 113, 115, 122.

- [ ] **Track 112 — Upstream cherry-pick sweep + issue re-triage.** Re-run
  the Track-108 sweep on `main..upstream/main` since `bbf6893c`
  (2026-08-19); fixes-only `-x` picks; append the triage table + new sync
  ref to `docs/project/upstream-issue-triage.md`. Watch list: PR #3381
  (Responses API — only if merged), `dingtalk-stream-sdk-go` stable fix
  for #3382, opencode-go follow-ups on #3371.
- [ ] **Track 113 — Requirement-aware mesh dispatch** (core).
  - [ ] `mesh.Requirements{Models, Skills}` + filter in `capabilityServes` /
    `rankedCandidates` — a manifest that omits a class never satisfies it
    (same posture as `swarm offer --require-*`); no new `Capability` fields
  - [ ] `RemoteCall.Requires` → failover candidates; `FanoutRequest.Requires`
  - [ ] `--require-model` / `--require-skill` on `mesh route` + `mesh scatter`
    (not delegate/spawn/task — explicit-peer verbs have no pick)
  - [ ] docs: operators set `mesh.advertise_models`/`advertise_skills` for
    requirement-filtered routing to see them
  - [ ] tests: filter unit tests per class; absent-field-unmatched; scatter
    branch filtering; "no capable peer" error path
- [ ] **Track 114 — Inbound capacity controls** (core).
  - [ ] `mesh.max_concurrent_tasks` (default 8) +
    `mesh.acl[].max_concurrent` per-peer override; saturate →
    `StatusRejected` with a `"capacity:"` class
  - [ ] caller side: `capacity:` rejection skips same-peer retries, fails
    over to the next ranked candidate (not added to `nonRetryable`)
  - [ ] `evictLocked` stays the backstop; tests: global cap, per-peer cap,
    failover-steering, config validate; `config.example.json`
- [ ] **Track 115 — Swarm claim scoring + subtask requirements** (swarm).
  - [ ] `Swarm.SetScoreLookup(func(peer.ID) (mesh.PeerScore, bool))` wired
    in `pkg/gateway/swarm.go` → `resolveOffer` ranks claims score desc →
    `ActiveTasks` asc → claim order; unscored claimants rank last
  - [ ] `OfferInfo` gains `Claims []ClaimInfo` (claimant, cap_digest, score,
    active_tasks); `swarm offers --json` shows pick rationale
  - [ ] `Subtask` gains `Requires`/`Tools`/`Model`/`Timeout`; `RunGoal`
    forwards to `OfferRequest`; decomposer prompt documents the fields;
    `parseSubtasks` passes them through
  - [ ] tests: scored-pick ordering, tie-break, requirement forwarding,
    decomposer round-trip
- [ ] **Track 116 — Restart reconciliation + run lifecycle** (swarm ops).
  - [ ] `runStore.Load` sweeps `status:"running"` → `"interrupted"` +
    `swarm.run.interrupted` event (taskstore precedent)
  - [ ] published offers persist to `swarm-offers.jsonl` (bounded ~256);
    `Start` re-drives open ones via `retryOffer` w/ `Attempt++`;
    TTL-elapsed → `expired` directly
  - [ ] `swarm run-cancel <run-id>` (CancelOffer on pending/offered
    subtasks) and `swarm run-retry <run-id>` (re-offer failed/expired/
    dead_letter/timeout subtasks w/ deps done; new `RunRecord` +
    `retry_of` link)
  - [ ] tests: restart sweep, offer re-drive, run-cancel, run-retry
    dep-gating
- [ ] **Track 117 — Coordinator & roster diagnostics** (swarm ops).
  - [ ] `SwarmInfo` gains epoch-age / last-state-write fields; `swarm status`
    surfaces coordination freshness
  - [ ] `rhizome swarm doctor <id>` — `MsgQuery` each member (bounded
    `max_members`, 5s/member), roster diff (known vs who-list-us),
    coordinator reachability; additive `queryRespPayload` epoch field
  - [ ] `swarm.coordination.prefer_connected` (default off) — impl-time
    evaluate; deterministic election stays default
  - [ ] tests: roster-diff fixtures, disconnected-member detection,
    election determinism unchanged
- [ ] **Track 118 — Observability depth** (mesh + swarm).
  - [ ] `mesh.activity_log` (default true) →
    `<RHIZOME_HOME>/mesh-activity.jsonl`, size-rotated like the audit log;
    `mesh activity --since` reads across restarts
  - [ ] filters: `mesh activity --kind/--peer/--swarm/--since` + matching
    `GET /network/activity` query params
  - [ ] `mesh trace <task-id>` + `swarm trace <offer-id|run-id>` —
    correlate taskstore/runstore + `mesh-audit.jsonl` + activity events
  - [ ] `PeerScore` per-op counters + read-time decay via
    `mesh.score_half_life` (default 168h); per-op breakdown in
    `PeerScoreView` + `mesh peer <id>`; `recordPeerCall` carries op labels
  - [ ] tests: JSONL persistence + rotation, filter behavior, trace
    correlation, decay math, per-op splits
- [ ] **Track 119 — Dashboard/API depth** (web + gateway).
  - [ ] daemon: `GET /network/trace`, `GET /swarm/<id>/health` (doctor
    payload), claim detail on `/swarm/offers`
  - [ ] web: activity-panel filter bar; peer-detail per-op score
    breakdown; swarms-panel health card (coordinator, epoch age, member
    asymmetry); tasks-panel `retry_of` linkage
  - [ ] API surface lands regardless; web half is the declared cut line
- [ ] **Track 120 — Transfer & skill depth** (flex — first cut line).
  - [ ] blob resume: offset-resume (or chunk framing) inside
    `/rhizome/blob/1.0.0` + sha256 dedup skip; single-shot fallback for
    peers without it
  - [ ] `mesh blobs` verb — local store listing + TTL state
  - [ ] optional `mesh skill push` (notify → peer pulls); impl-time
    keep/drop
  - [ ] tests: resume byte-integrity fixture, dedup, fallback
- [ ] **Track 121 — Integration CI + flake automation** (ops).
  - [ ] `.github/workflows/integration.yml` — weekly cron +
    `workflow_dispatch`, runs `integration-mesh.sh` +
    `integration-swarm.sh` on Linux, uploads logs; advisory posture (like
    `test-parallel`), never a merge gate
  - [ ] flake-hunt promotion criteria recorded; README/AGENTS note
- [ ] **Track 122 — Docs & release v0.15.0** (docs/release).
  - [ ] `docs/guides/spawn-tasks.md` + new `docs/guides/swarm-ops.md` +
    `docs/operations/debug.md` additions
  - [ ] `config.example.json` (each new field lands on its owning track);
    `AGENTS.md` command/config/file sync; `ROADMAP.md` §4; `.todo.md`
    rotate; `docs/release-notes/v0.15.0.md`
  - [ ] tag `v0.15.0` — **explicit go-ahead only**

### Acceptance criteria

- [ ] `go build -tags goolm,stdjson ./...`; `go test -p 1 -tags
  goolm,stdjson ./...`; `golangci-lint` v2.13.2 → 0 issues;
  `lint-docs.sh` OK; no new Go dependencies
- [ ] v0.14.0 released + verified (archives, GHCR pull, `rhizome version`)
- [ ] `--require-*` filters picks per manifest advert; absent class never
  matches; no new `Capability` wire fields
- [ ] `capacity:` rejection fails over to the next peer without burning
  same-peer retries; lands in audit + score as failure
- [ ] score-weighted claim pick; `swarm offers --json` shows claim detail;
  subtask requirements reach `OfferRequest`
- [ ] restart: run sweep → `interrupted`, open offers re-drive w/
  `Attempt++`; `run-cancel`/`run-retry` dep-gated per spec
- [ ] `swarm doctor` reports roster asymmetry + coordinator reachability;
  `swarm status` shows epoch freshness
- [ ] activity persists across restarts; filters work; `mesh/swarm trace`
  correlate lifecycle; `mesh peer` shows per-op + decayed score
- [ ] blob resume byte-identical on restart; `mesh blobs` lists store
- [ ] integration workflow runs green once (advisory); triage doc records
  the new sync ref
- [ ] release: docs updated, `config.example.json` validates, release
  notes + tag

---

## Planned — v0.16.0

v0.16.0 is **Market Graduation** — the market tail (Track 105 + the Track
104 E2E tail, deferred at both prior revisions) plus **all four Stage-D
items pulled in at the megaplan**: session drawdown, decentralized
arbitration, TEE/attestation, portable signed reputation. Design record:
`docs/design/v0.16.0-sprint.md`. Track numbers: 123–136 (14 tracks — the
largest sprint). Slipping v0.14.0/v0.15.0 tracks prepend this queue. Cut
lines (in order): 131 → 135 → 134 → 133 → 126 → 130 → 128 → 129. Never cut:
123, 124, 125, 127, 132, 136.

- [ ] **Track 123 — Upstream cherry-pick sweep + issue re-triage** (ops;
  first). `main..upstream/main` since Track 112's sync point; fixes-only
  `-x` picks; triage table + new sync ref in
  `docs/project/upstream-issue-triage.md`. Watch list: #3381 (if merged),
  DingTalk SDK fix for #3382, opencode-go follow-ups.
- [ ] **Track 124 — Market reputation substrate** (core seam).
  - [ ] `PeerScore` market-outcome fields riding the v0.15.0-118 `OpStats`
    shape (`market_buy`/`market_sell` op labels: outcome counts +
    `session_id` ref + `value_hash` commitment — **no raw amounts**)
  - [ ] bridge non-splice action `peer_score {peer, op, outcome, ref}` —
    token-gated, per-module allowlisted, bounded; daemon writes the store
  - [ ] module records outcomes at settle (release/claim/resolve); disputes
    record pending until resolved
  - [ ] `PeerScoreView` + `mesh peer <id>` show the market split
  - [ ] tests: verb auth/bounds, outcome write, view surface
- [ ] **Track 125 — Curated provider index** (ops/infra).
  - [ ] `stpinkie/rhizome-market-index` repo: `index.json` + `.sig`
    (Ed25519, `MODULE_CATALOG_SIGNING_KEY` pattern), envelope `{signer,
    seq, expires_at}`, entries `{peer_id, addrs, offers, payout,
    runtime_posture, attestations[], manifest_sig}`; 1 MiB / 500 bounds
  - [ ] publish CI (validate + seq bump + sign on tag);
    `docs/operations/market-index-signing.md` (skill-index-signing
    posture); admission policy doc (single first-party curator v1,
    federation deferred)
  - [ ] `market_index_url` default → first-party index at graduation
- [ ] **Track 126 — DHT rendezvous tier `rhizome-market-v1`** (core seam +
  module).
  - [ ] bridge actions `dht_provide`/`dht_find` (JSON req/resp, not
    splice; `ns` prefix-locked to `rhizome-market-*`) → daemon
    `Provide`/`FindProviders`
  - [ ] module client + `market.dht` field (default off; requires host
    `dht.enabled` — clean error, not a hang)
  - [ ] `market find` merge precedence index → DHT → cache; `unvetted`
    marker on DHT results everywhere
  - [ ] loopback two-peer provide→find→connect fixture (mDNS/NAT isolated)
- [ ] **Track 127 — Escrow graduation** (module). Execute the Track-101
  survey pick; **design the graduated contract surface once** —
  `open` + `release(sessionId, amount)` partials (drawdown-ready) +
  `claim` + `dispute`/`resolve` + ERC-792 evidence/ruling hooks
  (arbitration-ready); Sepolia deploy, source-verified; arbiter v1 =
  designated multisig (governance doc); security-review checklist
  committed.
- [ ] **Track 128 — Session-drawdown settlement** (Stage-D).
  - [ ] `escrow.settlement=per_task|drawdown` + `session_budget`/
    `draw_interval` fields
  - [ ] `open` once w/ budget → per-receipt partial `release` → `close`
    refunds remainder; module drawdown ledger + `buy_max_cost_*`
    enforcement
  - [ ] fixtures: partial-release sequences, budget exhaustion, dispute
    mid-drawdown; vouchers documented as the rejected alternative
- [ ] **Track 129 — Decentralized arbitration** (Stage-D).
  - [ ] `escrow.arbiter` = `address` (designated, default — never removed)
    or `kleros:<court>` (ERC-792/1497 escalation per the 101 survey)
  - [ ] `dispute` escalates w/ fees; evidence = `_rhizome.receipt` + terms
    hash via `submitEvidence`; ruling → `sellerBps` at `resolve`
  - [ ] dispute-desk ops doc (filing, evidence format, timeline, fees)
- [ ] **Track 130 — Portable signed reputation** (Stage-D).
  - [ ] attestation schema `{provider_peer_id, session_id, outcome,
    terms_hash, task_hash, issued_at, buyer_peer_id, signature}` — node
    key signed
  - [ ] `market attest <session>` issues (buyer opt-in, never automatic);
    `market attest verify` validates sig→peer→hash chain
  - [ ] sellers carry newest-N (~32) in `advert.json.attestations` +
    index entries; issued attestations record via the `peer_score` verb
  - [ ] honest-limits doc: additive evidence, sellers can withhold
    negatives — not a ledger
- [ ] **Track 131 — TEE/attestation** (Stage-D; design + posture only —
  first cut line). `docs/design/tee-attestation.md` (TDX/SEV-SNP/
  TrustZone survey + verification depth); emit-when-set `attestation
  {kind, report_url|evidence_hash}` advert field; platform probe in the
  advert writer; `market session` displays claims. **No attested
  execution.**
- [ ] **Track 132 — Recorded two-node testnet E2E** (the Track-104 tail;
  the graduation gate). Sepolia, two real nodes: buy → `approve`+`open` →
  session → receipt verify → `release`; `dispute` → `resolve` (adapter if
  129 landed, designated otherwise); `claim` after the window; drawdown
  session if 128 landed. **Tx hashes in acceptance notes** (v0.12.0
  convention).
- [ ] **Track 133 — Buy-side depth** (module). `market buy --redundant N`
  (N providers, separate escrows, compare results — the threat-model
  redundancy mitigation); `market sessions` listing + `buy_max_cost_*`
  spend reporting.
- [ ] **Track 134 — Sell-side hardening** (module). Egress enforcement
  verified by fixture (container `network:none|allowlist`, sandbox net
  knob); per-peer rate-limit + concurrent-session audit; posture claims →
  131 advert field; `market-audit.jsonl` completeness pass.
- [ ] **Track 135 — Market dashboard depth** (web). Sessions +
  reputation + dispute panels on the Network page; API lands regardless,
  web half is a cut line.
- [ ] **Track 136 — Docs & release v0.16.0** (docs/release).
  - [ ] `docs/guides/market.md` → "testnet-verified, experimental";
    discovery tiers, drawdown, arbitration, attestation sections
  - [ ] ops docs: index-signing (125) + dispute-desk (129);
    `mesh-economy-module.md` status → graduated
  - [ ] `config.example.json` (each field on its track branch);
    `AGENTS.md` sync (bridge verbs, market fields, attest verbs);
    `ROADMAP.md` §9 marks Stage C + D shipped; `.todo.md` rotate;
    `docs/release-notes/v0.16.0.md`; tag `v0.16.0` (explicit go-ahead)

### Acceptance criteria

- [ ] Build/test/lint gates green (`goolm,stdjson`; golangci-lint v2.13.2;
  `lint-docs.sh`); no new Go deps in core (module-side deps may be
  justified per track — contract bindings)
- [ ] v0.14.0 + v0.15.0 released + verified
- [ ] **Graduation gate**: recorded two-node Sepolia E2E w/ tx hashes in
  this file (buy→release; dispute→resolve; claim after window; drawdown
  session if 128 landed)
- [ ] `market find` resolves via curated index; DHT provide→find works
  loopback; merge precedence + `unvetted` marking verified
- [ ] settled session writes a market outcome via `peer_score`; `mesh
  peer` shows the split; no raw amounts on disk
- [ ] escrow contract source-verified; fixture rail stays the unset
  default; bound rail verified
- [ ] drawdown settles multi-task session on one `open`; arbiter
  escalation reaches `resolve`; attestations verify + refusable; TEE
  doc + advert field land
- [ ] `market buy --redundant` runs N escrows + compares; sell-side
  egress asserted; `market-audit.jsonl` complete
- [ ] release: docs updated, `config.example.json` validates, release
  notes + tag

---

## Queued — v0.17.0+

- [ ] AIEOS: continued exploration of AI-native OS interaction paradigms.
- [ ] Android device control — **blocked** on test hardware.
- [ ] Intra-mesh bilateral-ledger seed — `docs/design/mesh-economy-module
  .md`'s appendix; paired-operator settlement, if ever picked up.

## Completed (archive)

- All Phase 1 rebrand/docs, identity/security, network/CLI, build/release,
  hardware-compat, CI/CD, and v0.4 portability items.
- v0.5.0: `require_signed_caps` default true; nonce/timestamp required on
  `/rhizome/agent/1.0.0`; stale.yml cron; README news.
- v0.6.0–v0.6.2 (mesh operations): protocol unit tests; daemon/launcher
  task+audit APIs; Network Tasks/Audit panels; `active_tasks` in caps;
  `Mesh.PickPeer` + `mesh route`; `mesh-tasks.jsonl` persistence; task SSE;
  peer score store + retry/failover.
- v0.7.0 Swarm Mode (Tracks 21–30): `pkg/rhizome/swarm`, scatter-gather,
  presence, offer/claim queue, coordinator election, `swarm run`, Swarms
  panel, gossipsub transport; `integration-swarm` scripts.
- v0.7.1 (Tracks 31–32): stabilization & security (SSRF blocklists,
  prompt-injection guard, redaction, fs sandbox, mesh/swarm test
  stabilization, golangci-lint gate) + browser automation backends
  (`pkg/browser` catalog, endpoint resolvers, `tools.browser`, install
  manager, web `/browser` page, stealth skill) +
  `docs/architecture/footprint-audit.md`.
- v0.8.0 (mesh/swarm depth): blob transfer, task `--attach` attachments +
  result artifacts, media tools (`vision_mode`/`load_video`/
  `transcribe_audio`), offer cancel/retry/dead-letter + capability-aware
  claims, orchestration v2 (DAG waves, run records, dashboard run panel),
  `/rhizome/pair/1.0.0` trust pairing, mesh skill distribution, MCP
  context7 preset, `pkg/browser/cdp` native CDP client.
- v0.8.1–v0.8.5: release-readiness hardening; small-VM validation path
  (`validate-small-vm.sh`, `lint-slim`, `build-core`); swarm re-announce
  roster healing; `Swarm.save` serialization; `ReliableConn.ReadFrame`
  drain fix; stderr→stdout swarm CLI fix; `SkillShare` race fix; pnpm
  format gate.
- v0.9.0 (Tracks 54–59): upstream defect burn-down (config multi-key
  collapse, sensitiveCache race, Telegram animator, QQ 401, session
  archive, web chat lag, channel flat-key compat); attachment matrix;
  onboarding wizard + web `/setup`; swarm blackboard; signed agent
  manifests; `session_header`, `openai-compatible` preset, IRC splitting.
- v0.9.1: `Syncer.Stop` WaitGroup panic fix; swarm claim-window drain;
  test-timing race fixes; lint sweep; small-VM OOM caps; **first tag with
  real release artifacts**.
- Track A (release plumbing): `release.yml` on `push: v*`; `create-tag.yml`
  dispatch; `nightly.yml` Docker-Hub-optional; `goreleaser check` in
  pr.yml; `release-smoke.yml` weekly; winres snapshot fix; smoke run caught
  the netbsd `Bsize` blocker.
- Track B (parallel test flakes): mDNS/NAT isolation + `testutil.NewIdentity`;
  deterministic swarm queue tests; `scripts/flake-hunt.{sh,ps1}` + advisory
  `test-parallel` job; `TestMDNSDiscoversPeer`.
- Track C (footprint philosophy, partial — tails moved to Track 64):
  "ultra-lightweight"→ubiquity re-spec; doc sweep (README + 9 locales +
  CONTRIBUTING + workspace docs); binary-size gate in `build.yml`.
- v0.10.0 (Tracks 60–66): companion-module system (`pkg/modules`, `rhizome
  module`, `/modules` page); ACP server (`rhizome acp`) + ACP client
  (`agents.list[].acp`, `acp_run`); mesh observability (conns/score/
  bandwidth/activity feed, `mesh peer`/`mesh activity`, dashboard panels);
  worker-node tier + `measure-footprint.sh` (worker ≈50 MB / full ≈55 MB);
  `nimbus-verified-proxy` + `ethereum-rpc` first tenants (P2P LC default);
  ROADMAP re-eval + guides + release notes.
- v0.11.0 (Tracks 67–68, 70–71 — merged scope): shipped web3 Stage 2 reads
  (`pkg/web3` client/resolver, 7 read-only `web3_*` tools, SSRF posture,
  30 s endpoint cache); module trust (Ed25519-signed catalog + opt-in
  remote index `module_index` + `module verify` drift detection + secret-
  gated release signing); new tenants (`helios` linux/darwin, `ipfs-kubo`
  all platforms; `.zip` extraction, per-GOOS `AssetTemplates`,
  `{module_dir}` placeholder, `init_marker`/`init_args`/`setup_args`,
  catalog schema v2). Open Tracks 69/72/73/74/75 moved to v0.12.0 as
  Tracks 77/81/80/82 (75 absorbed into 76+83).
- v0.12.0 (Tracks 76–83, "Agentic web3"): wallet + gated signing
  (secp256k1 keys, EIP-1559, durable pending-approval queue, CLI/daemon/
  dashboard approvals); contract interaction (ABI registry,
  `web3_contract_call/send`, ERC-20/721, ENS); role-aware mesh routing +
  dashboard topology; ACP `session/load` persistence + client-declared
  MCP passthrough + `terminal/*` policy bridge; `enc2://`
  XChaCha20-Poly1305 credentials + OAuth headless matrix. Sepolia E2E
  verified.
- v0.13.0 (Tracks 84–96, "Agent interop depth"): ACP session modes +
  per-session model/config options + external-agent authMethods +
  session persistence/media/mode forwarding + mcpCapabilities;
  `rhizome mcp serve`; signed curated skill index (`pkg/sigverify`);
  remote-task usage metering (`want_usage`/`usage` wire fields); module
  trust tail (catalog schema v3 release signatures) + llama.cpp design
  doc; enc2 key-store migrations (`node.json`, `web3/keys.json`); CI PR
  labeler + concurrency. Tagged; `docs/release-notes/v0.13.0.md`.
  (Integration-suite CI signal carried into v0.14.0 Track 111.)
