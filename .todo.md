# Open Items

Only genuinely open work is tracked here. Completed Phase 1 leftovers and the
v0.4 portability phases are collapsed below — see git history for details.

## In progress — v0.7.1 (Tracks 31–32)

Theme: **Stabilization, Security & Footprint Prototype**.

### Track 31 — Stabilization & Security
- [x] Fix flaky `TestMeshRemoteCall` and other mesh/swarm integration test races.
- [x] Stabilize `scripts/integration-swarm.ps1` and `scripts/integration-swarm.sh` (verified green end-to-end on Windows).
- [x] Decide fate of `origin/fix-golangci-lint-failure-5375077918160686143` and `origin/freebuff/go-through-the-github-actions-to-ass-vx8djdtz` branches — **superseded, kept for reference**: both are CI-fix attempts that diverged before the v0.7.0 swarm work; their lint/CI fixes were applied to `main` in a different form (`4ac5b2d5`, `1936af9e`). Upstream (`picoclaw`) branches are not ours to prune.
- [x] Make `golangci-lint --build-tags=goolm,stdjson` pass (CI pinned to golangci-lint v2.13.2) and `make lint` target wired up.
- [x] SSRF blocklist for URL-bearing tools: `web_fetch` blocks private/loopback/link-local/metadata IPs at pre-flight *and* dial time (incl. IPv4-mapped IPv6, 6to4, ISATAP, redirect targets, `private_host_whitelist`); `exec` scans embedded URLs; `browser_open`/snapshot/screenshot URLs reject private hosts literally and after DNS resolution, via `tools.browser.private_host_whitelist` (initial URL only — in-page redirects/subresources follow normal browser rules).
- [x] Prompt-injection hardening: shared patterns in `pkg/guard`; shell commands block on match; tool-call JSON extracted from free-text model output drops injected calls; registry warn-scans all tool args (nested + arrays).
- [x] Privacy redaction: `pkg/redact` (Bearer/API-key/token/sk-* patterns + Authorization headers) applied to logger messages and fields; configured `SecureString` secrets are masked via `SetGlobal` → `redact.SetExtraMasker`; tool results pass `FilterSensitiveData` + generic patterns.
- [x] Enforce filesystem sandbox: `restrict_to_workspace` (default true) covers read_file/write_file/edit_file/append_file/list_dir/exec via `buildFs`, with allow_read_paths/allow_write_paths whitelists; verified wiring in `agent_init.go`/`instance.go`.
- [ ] Replace `assets/logo.webp` with actual Rhizome branding. **Blocked**: needs a real logo asset — no art pipeline exists; keep the placeholder for now.
- [ ] Replace PicoClaw mascot/slogan/shrimp emoji in translated READMEs.
- [x] Refresh translated README news sections up through v0.7.0 (9 locales).
- [ ] Re-enable `nightly.yml` — **blocked**: release credentials (signing/notarization secrets) are not configured in the repo; documented in release notes.
- [ ] Re-add `docker-build.yml` — **blocked**: same credentials gap as nightly.

### Track 32 — Browser Automation Backends & Memory Audit
- [x] `pkg/browser` backend catalog (12 backends) + `agent-browser` CLI driver (local Chromium; `AGENT_BROWSER_CDP` env for remote endpoints so credentialed ws URLs stay out of argv; `-p` provider plugins + env).
- [x] Endpoint resolvers: system Chrome/Edge (spawn + `--remote-debugging-port`), ClawBrowser (BYO `clawctl`), custom CDP URL, Steel/Hyperbrowser/TinyFish session APIs, Cloudflare REST (stateless snapshot/screenshot).
- [x] `tools.browser` config section (default backend, session timeout, per-backend SecureString keys) + `browser_open`/`browser_snapshot`/`browser_click`/`browser_fill`/`browser_screenshot`/`browser_eval`/`browser_wait`/`browser_close` tools with SSRF guards.
- [x] Install manager (agent-browser npm install; detect-only for ClawBrowser/system browser) + disk-space gating.
- [x] Web admin `/browser` page: backend cards, license badges, install/uninstall, default selector, API-key fields.
- [x] Stealth path: `docs/guides/browser-automation.md` + `workspace/skills/stealth-browser` skill (Patchright MCP server).
- [x] Memory audit → `docs/architecture/footprint-audit.md`.
- [x] Write `docs/release-notes/v0.7.1.md`.
- [x] Review hardening: `PUT /api/browser` preserves fields not sent (session_timeout/whitelist/backends) and validates session_timeout; idle sessions reaped in background; `CloseAll` serializes with in-flight calls without blocking shutdown; secret-looking `env` values masked via `SensitiveDataReplacer`; version lookups cached; npm install bounded by timeout; disk-space gate checks the install volume.
- [x] Post-review fixes: `redact.MaskAny` masks composite log fields (maps/slices incl. tool args) and secret-keyed values; old agents' `BrowserMgr` released on config reload; CDP endpoint passed via env not argv; system Chrome binds `--remote-debugging-port=0` via DevToolsActivePort; backend status reports driver presence.

Notes: `nonetwork` build re-dropped — it removes the mesh/swarm features that differentiate Rhizome from PicoClaw (first dropped in b6764ac0, re-affirmed). Dropped browser backends: LightPanda (AGPL, no renderer/Windows build), CamouFox (Playwright protocol, Python bridge), Donut (AGPL, Pro-gated CDP). Deferred to v0.8.0: own minimal Go CDP client (enables Cloudflare header-auth sessions + Node-free path), patchright Node bridge.

### Acceptance criteria
- [ ] `go build -tags goolm,stdjson ./...` and `go test -tags goolm,stdjson ./...` green on Linux and Windows for three consecutive runs.
- [x] `golangci-lint --build-tags=goolm,stdjson` passes (0 issues with golangci-lint v2.13.2; `make lint` also runs `scripts/lint-docs.sh`).
- [ ] Mesh and swarm integration tests pass without flakes. `scripts/integration-swarm.ps1` verified green. **Deferred**: `TestMeshFanoutQuorumPending`/`TestSwarmOfferClaimAssign`/`TestMeshTaskSpawnDisabled` pass in isolation but flake under full-suite parallel load on Windows — suspected Windows Firewall prompts delaying libp2p listeners. Re-verify on a Linux host before further tuning.
- [x] SSRF and prompt-injection unit tests added and passing (`pkg/redact`, `pkg/guard`, `pkg/browser`).
- [x] Browser tool can open a URL, snapshot interactive elements, and perform a click/fill via the agent-browser backend; backend install/default selection works from the web console (driver arg construction + session lifecycle unit-tested; live run requires `npm i -g agent-browser`).
- [x] README translations and release notes updated.

## Deferred to later phases

- [ ] Phase 3 — qemu 32-bit mesh tests and real-device Android smoke tests (deferred; current environment lacks QEMU / Android 4.4 test hardware). Note: `ANDROID_API` is now 21 — NDK r26+ dropped API <= 20 toolchains, so Android 4.4 (API 19) can no longer be built regardless. Go 1.26.0–1.26.2 also had a seccomp regression on 32-bit Android <= 10 (fixed in 1.26.3+).
- [ ] Skill marketplace / `find_skill` registry discovery (mesh skill distribution shipped in v0.8.0).
- [x] Linux re-verify of the Windows-flaky mesh timing tests. **Done 2026-09-12** — Linux shows the same pattern: `TestSwarmOfferRetryThenDeadLetter`, `TestSwarmOfferCancel`, `TestMeshFanoutAll`, `TestSyncerConflictingEditsConverge` flake under parallel `go test` but pass with `-p 1` and in isolation, so the flakiness is scheduler/dial-timing contention, not Windows Firewall. `TestMeshFanoutQuorumPending`, `TestSwarmOfferClaimAssign`, `TestMeshTaskSpawnDisabled` passed in every Linux run.

## Completed (archive)

- v0.8.0 (mesh/swarm depth) sprint: `/rhizome/blob/1.0.0` trusted content-addressed file transfer; remote task `--attach` attachments + result artifacts; `vision_mode`/`load_video`/`transcribe_audio` media tools; swarm offer cancel/retry/dead-letter + capability-aware claims; orchestration v2 (DAG `depends_on` waves, `swarm-runs.jsonl`, `swarm runs`/`run-status`, dashboard run panel); `/rhizome/pair/1.0.0` trust pairing (single-use signed bundles, `network pair --create/--accept`, web panel); mesh skill distribution (`mesh.skill_share` allowlist, `/rhizome/skill/1.0.0` + blob transport, guard-scanned install, `.skill-origin.json` mesh provenance); `tools.mcp.presets.context7` preset with SecureString key + CLI (`mcp preset`) + web card; `pkg/browser/cdp` native Go CDP client + `rhizome-cdp` backend with authenticated-WSS (Cloudflare) support; notes in `docs/release-notes/v0.8.0.md`.

- All Phase 1 rebrand/docs, identity/security, network/CLI, build/release, hardware-compat, CI/CD, and v0.4 portability items.
- v0.5.0 sprint: `require_signed_caps` now defaults to `true`; `nonce`/`timestamp` are required on `/rhizome/agent/1.0.0` (legacy requests rejected); `stale.yml` daily cron re-enabled; README news refreshed.
- v0.6.0 (mesh operations) sprint: protocol unit tests for `agentrpc`/`agenttask`; daemon `GET/POST /network/tasks` + `GET /network/audit`; launcher `/api/network/tasks` + `/api/network/audit` with CLI/file fallback; Network page Tasks + Audit panels; `rhizome mesh audit`/`network audit`; `active_tasks` in signed capability manifests; `Mesh.PickPeer` + `rhizome mesh route`; notes in `docs/release-notes/v0.6.0.md`; README news updated.
- v0.6.1 (mesh follow-through part 1) sprint: persistent task store (`mesh-tasks.jsonl`) with restart-to-error handling; daemon `GET /network/tasks/events` SSE; launcher proxy `/api/network/tasks/events`; Network page Tasks panel uses SSE with toast notifications and falls back to 3s polling; docs in `docs/release-notes/v0.6.1.md`; README news updated.
- v0.6.2 (mesh follow-through part 2) completed: peer performance scoring store (`mesh-peer-scores.json`) and `Mesh.PickPeer` quality ranking; remote task retry/failover across capable peers via `Mesh.SubmitRemoteTaskWithPeer` and `Mesh.CallRemote`; `task_retries` and `task_failover` mesh config fields; validated peer list inputs in the web console Mesh config section.
- v0.7.0 Swarm Mode (Tracks 21–30) completed: `pkg/rhizome/swarm` (signed envelopes over `/rhizome/swarm/1.0.0`, join/leave + roster gossip, persisted `swarms.json`); `Mesh.FanoutTask` scatter-gather + `network scatter`/`mesh scatter`; presence heartbeats + member expiry; offer/claim work queue (`swarm offer|offers`); daemon `/network/swarms*` API + SSE + launcher `/api/network/swarms*` proxies; per-swarm ACL + rate limits + audit; deterministic coordinator election (lowest peer id) writing `swarm/<id>/state.json` into the synced workspace; `swarm run` goal orchestration via local-agent decompose → queue offers → synthesize; Swarms panel on the Network dashboard; optional `swarm.transport: "gossipsub"` backend (`go-libp2p-pubsub` v0.17.0); `scripts/integration-swarm.ps1|.sh` + `make integration-swarm`; notes in `docs/release-notes/v0.7.0.md`.
|- v0.8.0 (mesh/swarm depth) — see `docs/release-notes/v0.8.0.md`.
|- v0.8.1 (release-readiness hardening) — see `docs/release-notes/v0.8.1.md`.
|- v0.8.2 (stabilization and release gating) — see `docs/release-notes/v0.8.2.md`.

## In progress — v0.8.3 (Tracks 48–50)

Theme: **Post-validation stabilization** — no new features.

### Track 48 — Linux test fix and tag sync
- [x] Fix `pkg/evolution` `TestRuntime_RunColdPathOnce_DraftSaveFailureRollsBackAppliedSkill` for root-owned Linux containers.
- [x] Push the `v0.8.2` git tag to origin.

### Track 49 — Small-VM validation path
- [x] `make lint-slim` for in-scope packages.
- [x] `make build-core` and `scripts/build-core-targets.sh` for core cross-compile targets with per-target cache cleanup.
- [x] `scripts/validate-small-vm.sh` for 4 GB root-FS / 2 GB memory Linux VMs.
- [x] Document small-VM validation in `AGENTS.md`.
- [x] Re-run `scripts/validate-small-vm.sh` on the 4 GB cloud VM and capture the report. **Done** — report showed all 5 stages passed on the 4 GB overlay/2 GB container with `GOCACHE`/`GOMODCACHE` on a host mount.

### Track 50 — Release notes and versioning
- [x] `docs/release-notes/v0.8.3.md` written.
- [x] Tag `v0.8.3` on the final commit.

### Acceptance criteria
- [x] `go test -p 1 -tags goolm,stdjson -count=1 ./...` green on Windows.
- [x] `golangci-lint run --build-tags goolm,stdjson ./...` 0 issues.
- [x] `make lint-slim` green.
- [x] `scripts/validate-small-vm.sh` green on a 4 GB VM (with caches redirected to a large host mount).
- [x] `v0.8.2` tag present on origin.
- [x] `v0.8.3` tag present on origin.
- [x] Full release gate (lint all, race, integration, 15-target cross-compile) green on a Linux runner with ≥ 10 GB disk and ≥ 4 GB memory. **Done 2026-09-12** on an amd64 Linux cloud VM (124 GB disk, 31 GB RAM): sequential `go test -p 1` green (102 pkgs), `make lint` 0 issues, race suite (mesh/stream/swarm) green, Docker + mesh + swarm integration green, `make build-all` 15 targets green. Report: `.rhizome-tests/reports/rhizome-test-report-linux-amd64-devin-20260912-205007.md`. Found+fixed: `swarm members`/`list`/`status`/`offers` printing to stderr (broke `integration-swarm.sh` on POSIX), a `Mesh.cfg.SkillShare` data race, an `unconvert` lint failure, and 11 unformatted frontend files that failed the `pnpm format` CI gate on main. Note: mesh/swarm/sync timing tests still flake under default parallel `go test` on Linux too — pass with `-p 1` or in isolation.

## In progress — v0.8.4 (Tracks 51–53)

Theme: **Small-VM tooling hardening** — no new features.

### Track 51 — Fix build-core / build-all inversion
- [x] `make build-all` builds the full 15-target matrix.
- [x] `make build-core` builds only the 12 core targets via `scripts/build-core-targets.sh`.
- [x] Remove redundant `freebsd`/`openbsd` step from CI `cross-compile` job.

### Track 52 — Smart cache and build output management
- [x] `scripts/rhizome-small-vm-helpers.sh` added.
- [x] `build-core-targets.sh` reuses large-volume `GOCACHE` and removes binaries when `BUILD_DIR` is on the small overlay.
- [x] `validate-small-vm.sh` places `GOTMPDIR`/`TMPDIR` under `GOCACHE` and honors `RHIZOME_BUILD_DIR`.

### Track 53 — Release notes and versioning
- [x] `docs/release-notes/v0.8.4.md` written.
- [x] Re-run `scripts/validate-small-vm.sh` using the updated script. **Clean pass 2026-09-12** (583s, all 5 stages: build, vet, unit tests, lint-slim, core cross-compile). Caveat: do not run it concurrently with another libp2p test suite on the same host — tests share derived peer IDs and cross-dial.
- [x] Run `make build-all` on an `ubuntu-latest`-equivalent runner. **Done 2026-09-12** on a Linux amd64 VM: all 15 targets built in 330s.
- [x] Tag `v0.8.4` on the final commit (`f0e7339b`) and push to origin.

## In progress — v0.8.5 (post-v0.8.4 stabilization)

Theme: **full Linux release gate + the fixes it surfaced.** No new features.

- [x] Re-land the two v0.8.4-tag script fixes on `main` (`1d8dc6fd`): `env $env_vars` in `build-core-targets.sh`, `GOTMPDIR`/`TMPDIR` redirection in `validate-small-vm.sh`.
- [x] Linux validation run (PR #3): `swarm members|list|status|offers` stderr→stdout fix, `Mesh.cfg.SkillShare` race fix (`skillShareMu` + `SetSkillShare`), `unconvert` cleanup, `pnpm format` gate repairs, `shell.go` sandbox canonicalization (`EvalSymlinks` + `expandLeadingTildes`).
- [x] CI policy: GO-2024-3218 govulncheck exemption (with crash-fails-closed hardening); retry documented libp2p timing flakes (Windows packages ×2, swarm integration ×1).
- [x] Swarm periodic re-announce — rosters self-heal after a lost single-shot JOIN/JOIN_ACK (`reannounceLoop`, `TestSwarmRosterReconvergesAfterTrustGrant`).
- [x] `Swarm.save` serialized + unique tmp + rename-first — concurrent savers could delete `swarms.json` (`TestSaveConcurrentKeepsRoster`).
- [x] `ReliableConn.ReadFrame` drains `recvCh` before reporting closed — a delivered frame raced the peer's close and was dropped ~50% when both select cases were ready (`TestReadFrameDeliversFrameQueuedBeforeClose`); root cause of the `agentrpc` "reliable conn closed" flake and plausibly the lost JoinAcks above.
- [x] `docs/release-notes/v0.8.5.md` written.
- [x] Tag `v0.8.5` on the release commit and push to origin.

## In progress — v0.9.0 (Tracks 54–59)

Theme: **upstream defect burn-down + attachments + onboarding + multi-agent depth.**

### Track 54 — Inherited defect burn-down
- [x] `SaveConfig` multi-key model collapse — `collapseMultiKeyModels` merges `__key_i` virtuals into primary `APIKeys` preserving raw `enc://`/`file://` forms; orphan virtuals demote rather than drop (#3373).
- [x] `sensitiveCache` lazy-init race — package-level mutex around the pointer swap; `Config` cannot host `sync.Once` (value copies in `normalizedConfigForSave`) (#3374).
- [x] Telegram tool-feedback animator bounds — consecutive-error abort, `tool_feedback_max_duration` timeout, self-detach on exit (#3343).
- [x] QQ 401 — `RegisterReqFilter` rewrites resty ≥2.17 `Bearer` → `QQBot` scheme (#3365).
- [x] Session history archive — `rewriteJSONL`/`Compact` snapshot to `*.archive-N.jsonl` before rewrite, tracked in `SessionMeta` (#3351).
- [x] Web chat input lag — composer state moved into `ChatComposer`; `AssistantMessage`/`UserMessage` memoized (#3350).
- [x] Channel flat-key compat — `Channel.UnmarshalJSON` folds non-base keys into `Settings`; diagnostics walker exempt (#3355).
- [x] `docs/project/upstream-issue-triage.md` — provenance + relevance of all inherited upstream issues.

### Track 55 — Attachment matrix
- [x] `SendMedia` for whatsapp (bridge), whatsapp_native (whatsmeow), VK, DingTalk — `media://` resolve → local path → channel API.
- [x] Inbound media ingest for whatsapp_native (download before empty-content early return), VK (photo/doc/voice), DingTalk (`downloadCode` → `robot/messageFiles/download`).
- [x] `docs/channels/media-matrix.md` — per-channel in/out support matrix.

### Track 56 — Onboarding
- [x] Interactive `rhizome onboard` wizard — env-key detection (18 providers), Ollama detection, catalog list, custom OpenAI-compatible endpoint, secure key entry, connectivity probe; terminals only, `--non-interactive`/`--yes` untouched.
- [x] Web `/setup` first-run page + `GET /api/setup/status` (`needs_setup` = no credentialed model or no default); provider → key/base → model fetch → test → save + set default; once-per-session skip; en + zh locales.

### Track 57 — Swarm shared context (blackboard)
- [x] `pkg/rhizome/blackboard` — `notes.jsonl` (all members) + `context.md` (coordinator-curated) in the synced workspace.
- [x] `MsgNote` propagation over signed swarm envelopes; coordinator-only context writes; member validation.
- [x] `swarm_context` tool, `rhizome swarm context|note` CLI, daemon HTTP endpoints, `swarm.context` config, `RunGoal` digest injection.

### Track 58 — Agent identity manifests
- [x] `pkg/rhizome/agentmanifest` — signed Ed25519 `Manifest` (agent id, name, persona, models, skills, peer id, issued-at), fingerprint, atomic save, `LoadAll`.
- [x] Embedded in `Capability.agent_manifests` — no new wire protocol; model/skill fields honor `advertise_models`/`advertise_skills`.
- [x] Receiver verification: peer-binding + signature check after outer cap verification; forgeries dropped with `mesh.error`/`capability.agent_manifest` event.
- [x] Gateway wires source (registry → `Input`) and sink (`<workspace>/agents/<id>.manifest.json`, synced to peers).
- [x] Status surfaces: `agent_manifests` fingerprints in `PeerCapability` for `network status --peers` / saved-peers.

### Track 59 — Polish + release gate
- [x] `session_header` model field → `options["session_key"]` → header on every OpenAI-compatible request; agent injects `ts.sessionKey` into `llmOpts` (#3369).
- [x] `openai-compatible` catalog preset — named generic endpoint (no default `api_base`, `EmptyAPIKeyAllowed`, fetch-capable); wizard custom pick + providers.md updated (#3366).
- [x] IRC long-message splitting — `splitLineBytes` splits PRIVMSG lines at ≤400 bytes on rune boundaries, preferring spaces; fixes mid-UTF-8 truncation for CJK/emoji (#3287).
- [x] Flake hunt — parallel `go test` on mesh/swarm/stream reproduced the documented timing flakes (`TestMeshFanoutQuorumPending`, `TestMeshTaskLifecycle` stall, `TestSwarmOfferRetryThenDeadLetter`); all pass under `-p 1`. No new sprint regressions.
- [x] `go build -tags goolm,stdjson ./...` + `go test -p 1 -tags goolm,stdjson ./...` green on Windows (`CGO_ENABLED=0`); `go vet` clean on all touched packages.
- [x] `docs/release-notes/v0.9.0.md` written.
- [ ] golangci-lint v2.13.2 `make lint` — tool not installed locally; runs in CI / on the Linux release runner.
- [ ] Tag `v0.9.0` + push to origin.

## v0.9.1 — Post-release hardening patch

- [x] `Syncer.Stop` WaitGroup-reuse panic — lifecycle gate (`stopping` + RWMutex + `spawn()`); production shutdown race, caught by remote validation.
- [x] Swarm claim-window close could drop a buffered in-window claim → offer wrongly expired; `resolveOffer` now drains `claimsCh` once more after the window.
- [x] `TestSwarmOfferRetryThenDeadLetter` — claims injected via `onClaim`, no network hop; deterministic under CPU starvation.
- [x] `TestSetContextCoordinatorOnly` / `TestAgentManifestAdvertisedInCapability` — timing races fixed (converged election; per-manifest authenticity vs snapshot fingerprint).
- [x] `golangci-lint` v2.13.2 full run — 0 issues (formatter sweep + canonicalheader + perms/nolint).
- [x] `validate-small-vm.sh` — `GOMAXPROCS=2`, `GOMEMLIMIT=1500MiB` defaults (2GiB cgroup OOM fix).
- [x] Remote validation green: lint 0, all targeted packages pass, parallel flake hunt shows only documented family.
- [x] Tag `v0.9.1` at HEAD + push.

### Carried known issues (documented parallel-only flakes)
- `TestMeshFanout*`, `TestMeshTask*`, `TestSwarmOfferCancel`/`ClaimAssign`/`DoneResult`, `TestSyncerConflictingEditsConverge` — timing-dependent under parallel `go test`; all pass `-p 1`.

## In progress — Footprint philosophy & docs sweep (Track C)

Theme: **re-spec the inherited PicoClaw "ultra-lightweight" identity.** Rhizome's
goal is omnipresence on *meaningful* hardware — aging laptops, obsolete Macs,
old Android phones, Pi-class+ SBCs, free-tier VMs — not sub-64 MB
proof-of-concept boards. Lightweight as a constraint, never at the cost of
useful functionality.

- [x] **C1 — Philosophy reframe (approved).** Deleted the `<20MB` core / `64MB`
  board / `10MB RAM` claims outright. New §1 in `ROADMAP.md`: "Ubiquity: An
  Agent on Every Machine" — idle cost, cold start, and dep-budget discipline
  replace footprint minimization.
- [x] **C3 — Doc sweep.** `README.md` (title/tagline/intro/features/Android
  4.4→5.0 fix), all 9 `docs/project/README.*.md` taglines unified to the new
  slogan, `hardware-compatibility.md` ARMv7 row, `footprint-audit.md`
  positioning note, `CONTRIBUTING.md`, `workspace/AGENT.md`,
  `workspace/skills/rhizome-agent/SKILL.md`. (Locale h1 titles still carry
  localized "ultra-efficient" phrasing — folded into the existing open
  translated-README rebrand item.)
- [x] **C4 — Binary-size regression gate in `build.yml`.** Checked-in baseline
  (`scripts/binary-size-baseline.txt`, stripped linux-amd64 bytes); CI warns
  at +15%, fails at +30%; baseline ratchets via deliberate PR. Companion
  signal: module dep count printed in the same step.
- [ ] **Standing watch — runtime RSS creep.** The size gate only covers the
  binary. Re-run the C2 measurements periodically (each release, or after any
  large dependency/runtime change) and take stock of idle-RSS drift; Devin
  should prompt for this when it sees relevant changes land.
- [ ] **C2 — Footprint measurement (deferred).** `scripts/measure-footprint.sh`:
  stripped build; sample `/proc/<pid>/status` VmRSS to steady state (~60 s)
  for three profiles — one-shot CLI, worker node (`--no-dht --no-gateway`, no
  mesh infra roles), full daemon — plus cold-start timing; emit JSON → refresh
  `footprint-audit.md`. **Infra:** none new required — WSL or the existing
  small Linux VM covers amd64 reference numbers; QEMU user-mode gives
  arm64/riscv64 upper bounds; real boards only needed to publish "verified"
  claims in the HCL.
- [ ] **Worker-node tier (name approved; in-tree module, not a repo).** A
  mesh participant that consumes swarm/sync/task services without running
  infrastructure roles (no public DHT, relay service, AutoNAT service,
  gateway; keeps relay *client* for NAT, trusted-peer bootstrap, remote-task
  execution). Lives as a config profile + a `role` field on the signed
  `mesh.Capability` manifest — the role bit is the seam for later expansion
  (role-aware `PickPeer` routing, coordinator-election preference for full
  peers, claim filtering by node class). If measured RSS delta vs the full
  daemon is ~≥20 MB, document it as a third tier; otherwise ship as a
  "constrained peer" config note in `docs/guides/configuration.md`.
