# Open Items

Only genuinely open work is tracked here. Completed sprints are collapsed into
the archive — see git history and `docs/release-notes/` for details. Roadmap
context lives in `ROADMAP.md`.

## In progress — v0.12.0 (Tracks 76–83)

Theme: **Agentic web3.** Cut v0.11.0 at its merged scope, then complete Web3
Stage 2: wallet + gated signing (carried), contract interaction (ABI registry,
contract tools, ERC-20/721, ENS), and wallet/approval operations depth —
plus carried mesh depth, ACP deepening, and security polish. Design doc:
`docs/design/v0.12.0-sprint.md`.

The mesh-economy arc sketched during planning was **descoped**: deferred to
an optional module — seed doc `docs/design/mesh-economy-module.md`.

Cut lines if the sprint runs long (in order): Track 82 → the two stretches
inside 79 (log watches, channel approvals) → the dashboard half of 79 → the
topology half of 80.

### Track 76 — Cut v0.11.0 at merged scope
- [ ] `docs/release-notes/v0.11.0.md` scoped to merged work (67 v0.10.0
  re-cut, 68 web3 reads, 70 signed catalog + remote index + `module verify`,
  71 helios/kubo + catalog v2); explicit note that 69/72/73/74 → v0.12.0.
- [ ] `AGENTS.md`/`.todo.md`/`ROADMAP.md` sync incl. stale-item corrections
  catalogued in the v0.11.0 doc (OneBot + `find_skills` shipped; OAuth
  partially shipped).
- [ ] Tag `v0.11.0` + push → `release.yml`; verify archives, GHCR pull,
  `rhizome version` in an artifact. **Run only with explicit go-ahead.**

### Track 77 — Web3 wallet + gated signing (carried v0.11.0 Track 69)
- [ ] `pkg/web3/keys.go` — secp256k1 (`decred/dcrd/dcrec/secp256k1/v4`,
  already in module graph) + Keccak-256 addresses (`x/crypto/sha3`); store
  `<RHIZOME_HOME>/web3/keys.json` (dir `0700`) encrypted via the
  `pkg/rhizome/identity` KeyProvider posture (keyring default, passphrase
  fallback); import/generate; `--reveal` gated + masked. **No go-ethereum.**
- [ ] `pkg/web3/abi.go` — minimal ABI codec (address/uint/int/bool/bytes/
  string/bytesN + dynamic types for ERC-20/721); table-tested vs spec
  vectors (`transfer` = `0xa9059cbb`, `approve` = `0x095ea7b3`).
- [ ] `pkg/web3/rlp.go` — minimal RLP encode (string/list, len<56/≥56).
- [ ] `pkg/web3/tx.go` — EIP-1559 + legacy build/sign (RLP),
  `eth_estimateGas`, nonce via `eth_getTransactionCount`,
  `eth_sendRawTransaction`. Keys never leave the box.
- [ ] `pkg/web3/policy.go` — `tools.web3.signing = {enabled:false,
  allow_contracts, allow_methods, max_value_wei_per_tx,
  max_value_wei_per_day, chain_ids, from_addresses}`; bounded spend ledger
  `<RHIZOME_HOME>/web3-ledger.jsonl` (rotate ~10 MB × 3). `from_addresses`
  (v0.12.0 delta) bounds which local addresses may sign.
- [ ] **Approval queue** — `pkg/web3/pending.go`, file
  `<RHIZOME_HOME>/web3-pending.json` (bounded ~100, expired pruned on
  read, atomic write; daemon owns resolution when running — daemonless
  CLI last-writer-wins documented): `web3_send`/`web3_sign` pass policy →
  enqueue → return `{pending_id, summary}` → resolve via `rhizome web3
  approve|reject <id>` or daemon `POST /web3/approvals/<id>` (+ launcher
  proxy + dashboard badge → full panel in Track 79) → sign+broadcast.
  `hook.approve_tool` hooks get first pass via `HookManager.ApproveTool`;
  timeout/cancel → deny.
- [ ] Tools `web3_send|web3_sign|web3_approve|web3_wallet|web3_pending|
  web3_send_status` (`pkg/tools/web3/signing.go`; AND-gate on
  `tools.web3.enabled && signing.enabled` in `IsToolEnabled`); CLI
  `rhizome wallet create|import|list|reveal` + `rhizome web3
  pending|approve|reject` (`cmd/rhizome/internal/{wallet,web3}/`);
  daemon `pkg/gateway/web3api.go` + launcher `web/backend/api/web3.go`
  proxies + frontend badge; `web3.*` events → activity feed + audit.
- [ ] Canary posture: `signing.enabled` defaults false; docs are
  testnet-first with explicit warnings.

### Track 78 — Contract interaction (ABI registry + contract tools + ERC-20/721 + ENS)
- [ ] ABI registry `<RHIZOME_HOME>/web3/abi/` (dir `0700`, ~200 files,
  ~512 KB each): `<label>.json` `{label, address?, chain_ids?, abi,
  added_at}`; CLI `rhizome web3 abi add|list|show|remove`.
- [ ] `pkg/web3/resolve.go` — contract arg resolution: `0x` literal →
  registry label → ENS name; `allow_contracts` accepts labels.
- [ ] `web3_contract_call {contract, method, args[]}` — name+arity ABI
  lookup (ambiguity → error listing candidates) → encode → `eth_call` →
  decode → JSON. Read path, no approval.
- [ ] `web3_contract_send {contract, method, args[], value_wei?}` — encode →
  policy → pending-approval → sign+broadcast (Track 77 machinery).
- [ ] Policy: `allow_methods` accepts `"<label-or-addr>:<method-name>"`
  (resolves to selectors via registry at eval); raw selectors still match.
- [ ] `web3_erc20` (balance|allowance|transfer|approve) + `web3_erc721`
  (ownerOf|balanceOf|tokenURI|name|symbol|transferFrom) — embedded minimal
  ABIs; writes route through `web3_contract_send`'s policy+pending path.
- [ ] `web3_ens` forward + `--reverse` — universal resolver `eth_call`;
  local namehash (sha3 dep); chain-gated to resolver-deployed networks
  (impl-time: resolver addresses per chain).
- [ ] `web3.contract.call|send|rejected` events → activity feed + audit;
  `web3_rpc` stays read-only (gated tools are the only write surface).

### Track 79 — Wallet & operations depth
- [ ] `rhizome wallet set-default <addr>` / `rename`; `web3_wallet` marks
  default; `from` defaults to wallet default.
- [ ] Daemon `GET /web3/wallet` (addresses+labels+default — never key
  material) beside Track 77 endpoints; launcher `/api/web3/*` proxies w/
  file fallbacks for reads.
- [ ] Dashboard `web/frontend/src/components/web3/` + `web/backend/api/
  web3.go`: wallet card (addresses/default/balances), pending-approvals
  panel (approve/reject → POST), tx status view; Modules/Browser card idiom.
- [ ] (stretch) Log watches — `tools.web3.watches[]` bounded ~8, ≥30 s
  interval; daemon `eth_getLogs` poller (lookback ≤ `max_log_range`) →
  `web3.event` runtime events; `web3_watch` tool (list/add/remove).
- [ ] (stretch) Channel approvals — `/web3 pending|approve|reject` in
  `pkg/commands` honored only from `tools.web3.approval_channels[]`
  scopes (default empty); same events/audit as CLI approvals.

### Track 80 — Mesh depth (carried v0.11.0 Track 73)
- [ ] Role-aware `rankedCandidates`/`PickPeer` behind
  `mesh.routing.role_aware` (`RoutingConfig` under `MeshConfig`,
  non-omitempty bool like `NATTraversal`; default true): worker bonus
  ~`1<<19` — below the direct-conn term `1<<20`; infra ops prefer `full`.
- [ ] Coordinator preference: `Role` on `pingPayload` (additive — swarm
  payloads sign raw bytes, inner fields safe) + `Member`/`MemberInfo`;
  `capProbeFunc` returns `cfg.Mesh.Role` via `Mesh.Role()` (ripples:
  `queue.go`, `SetCapProbe` in `pkg/gateway/swarm.go`); `reelectLocked` =
  `sort.SliceStable` on `(roleRank, peerID)`. Mixed-version flap is
  transient/advisory — documented.
- [ ] Topology panel: `network/topology-panel.tsx` — hand-rolled SVG radial
  fed by `/api/network/status` `peers[].conns` (direction → arrow/dash,
  transport → edge color, latency → thickness, `role` → badge);
  click-through to peer detail Sheet; slots into `network-page.tsx`.
  No new JS dep.

### Track 81 — ACP deepening (carried v0.11.0 Track 72)
- [ ] `session/load`: advertise `loadSession:true`; implement SDK
  `AgentLoader.LoadSession` (`agent_gen.go:381`, auto-dispatched); persist
  `acpSessionId → {agent_id, session_key, cwd, …}` at
  `<RHIZOME_HOME>/acp-sessions.json` (empty `pkg/acp/sessions/` +
  `pkg/acp/memory/` dirs = intended homes — use or delete); replay
  `SessionStore.GetHistory` as `session/update` chunks; re-register in
  `s.sessions`; (optional) restore cached `*_always` decisions.
- [ ] MCP passthrough: honor `McpServers` on `session/new` + `session/load`
  (`McpServerStdio` → `MCPServerConfig`; http/sse/acp variants refused —
  caps not advertised); per-session `pkg/mcp` `Manager` (bounded ~4);
  namespaced tools (`mcp__<srv>__<tool>`); **`ToolRegistry` has no
  `Unregister`** — add it or session-guard tools on the session-scope ctx
  key; teardown on close/cancel + isolation test; declared-env-only
  hygiene.
- [ ] `terminal/*` client capability via the guarded shell path
  (`CreateTerminal`/`TerminalOutput`/`KillTerminal`/`ReleaseTerminal`/
  `WaitForTerminalExit` on `clientHandler`);
  `acp.client.terminal_policy` = `deny` (default) | `allow`; bounded
  output; advertise `ClientCapabilities.Terminal` only when allowed.

### Track 82 — Security polish (stretch)
- [ ] `enc2://` XChaCha20-Poly1305 in `pkg/credential` (`chacha20poly1305.
  NewX`; blob `salt(16)|nonce(24)|ct`) — same HKDF shape (info `-v2` bump
  = impl-time call), transparent dual-decrypt with `enc://`, new writes
  use `enc2:` — **one-way door** → documented re-encrypt path +
  release-note. (Web3 key store stays on the Track-77 AES-GCM KeyProvider
  posture; node-identity store too — migrations are follow-ups.)
- [ ] OAuth residual — per-provider headless matrix (codex →
  device-code; anthropic → `--setup-token`/manual; antigravity → verify
  or clear error) + `providers.md` table.

### Track 83 — Docs & release
- [ ] `docs/guides/web3.md` major update: wallet, policy fields (incl.
  `from_addresses`), approval flow, contract tools, ERC-20/721, ENS,
  watches, `approval_channels`; testnet-first throughout.
- [ ] `docs/design/mesh-economy-module.md` — seed doc for the deferred
  economy-as-module (written with the sprint plan).
- [ ] `config.example.json` sections: `tools.web3.signing`,
  `tools.web3.watches`, `tools.web3.approval_channels`, `mesh.routing`,
  `acp.client.terminal_policy`.
- [ ] `AGENTS.md` command/API sync (`rhizome web3 abi`, wallet verbs, new
  tools, `/web3/*` endpoints); `ROADMAP.md` §9 rewrite (Stage 2 complete;
  Stage 3 → optional-module pointer); `.todo.md` rotate.
- [ ] `docs/release-notes/v0.12.0.md`; tag `v0.12.0` + push.

### Acceptance criteria
- [ ] `go build -tags goolm,stdjson ./...` green; `go test -p 1 -tags
  goolm,stdjson ./...` green on touched packages; `golangci-lint` v2.13.2 →
  0 issues; `lint-docs.sh` OK; binary-size delta recorded per dep-adding
  track.
- [ ] v0.11.0 published + verified (archives, GHCR pull, `rhizome
  version`); scoped to merged tracks with moved tracks noted.
- [ ] Signing verified end-to-end on testnet: policy denies, cap
  enforcement, `from_addresses` scoping, pending→approve→broadcast,
  deny-on-timeout.
- [ ] Contract tools verified on testnet: abi add/list, `contract_call`
  read, `contract_send` via approval, `label:method` allowlist enforced,
  ERC-20 transfer end-to-end, ENS resolve on a supported chain.
- [ ] Dashboard wallet card + approvals panel drive a real approve/reject
  against the daemon.
- [ ] Role-aware ranking + coordinator preference unit-tested; topology
  panel renders live `/api/network/status` data.
- [ ] ACP `session/load` round-trips a prior session in a real stdio
  drive; mcpServers spawn + tools visible; terminal deny-by-default proven.
- [ ] `integration-mesh`/`integration-swarm` green; networked tests follow
  the AGENTS.md isolation rules.

## Planned — v0.13.0 (Tracks 84–93)

Theme: **Agent interop depth.** Land the v0.12.0 stack, then deepen both
directions of ACP (session modes + config options server-side;
authMethods + session persistence + media client-side), expose Rhizome
tools as an MCP server, and add a signed first-party skill index — plus
CI/locale polish and docs+release. Design doc:
`docs/design/v0.13.0-sprint.md`.

Cut lines if the sprint runs long (in order): Track 89 → Track 92 →
Track 86 → the media half of 88. Track 84 is never cut.

### Track 84 — Land v0.12.0
- [ ] Merge #29 + #19 (CI fixes + sprint doc).
- [ ] Merge #20 → tag `v0.11.0` → verify release (archives, GHCR,
  `rhizome version`). **Tag pushes need explicit go-ahead.**
- [ ] Merge web3 chain #21→#22→#23→#24 (auto-retargets; expected
  conflicts: `.todo.md`, `AGENTS.md`, `config.example.json`).
- [ ] Merge #25, #26, #27 (independent).
- [ ] Retarget #28 to `main`, merge → tag `v0.12.0` → verify release.
- [ ] `.todo.md` rotate reconciles with #28 on merge.

### Track 85 — ACP session modes (`session/set_mode`)
- [ ] `acpSession.mode` per-session override (empty → `s.policy`);
  `toolApprover` reads session mode first.
- [ ] Advertise `Modes` on `NewSessionResponse`/`LoadSessionResponse`:
  `ask`/`auto`/`read-only` mapping to prompt/allow/deny;
  `permission_policy=deny` caps the advertised set.
- [ ] `SetSessionMode`: validate id → set → clear cached `*_always`
  decisions (fail-closed) → `current_mode_update` notification.
- [ ] Persist `mode` in the `acp-sessions.json` record; `session/load`
  restores it.

### Track 86 — ACP session config options (`session/set_config_option`)
- [ ] `ConfigOptions` in session responses: `category:model` select over
  `cfg.ModelList[].ModelName`; `CurrentValue` = active model.
- [ ] `SetSessionConfigOption` drives the `/switch model` machinery via an
  injected `Options.ModelSwitcher` seam (agent-global scope — documented;
  per-session override is a follow-up needing `InboundMessage` plumbing).
- [ ] Unknown configId/value → invalid-params listing candidates.

### Track 87 — External-agent `authMethods`
- [ ] `ClientManager.spawn`: on advertised `AuthMethods`, pick
  `acp.auth_method` or first satisfiable → `Authenticate` before
  `session/new`.
- [ ] `env_var`: satisfiable iff required vars present in `binding.Env`;
  error names missing vars. `terminal`: only under
  `terminal_policy=allow` (Track-81 bridge). `agent`: refused clearly.
- [ ] Unsatisfiable/rejected auth → kill + descriptive error.

### Track 88 — External-agent session depth + media
- [ ] `agents.list[].acp.session_mode` = `oneshot` (default) |
  `persistent` — reuse one session per agent; `session/load` when
  advertised; close on `ClientManager.Close`.
- [ ] Media passthrough: `req.Media`/`media://` refs →
  `ContentBlockImage` when `PromptCapabilities.Image`; text-ref degrade
  otherwise.
- [ ] Per-binding `acp.permission_policy`/`terminal_policy` overriding the
  `acp.client.*` globals.

### Track 89 — `rhizome mcp serve` (MCP server surface)
- [ ] `pkg/mcp/server.go`: `mcp.NewServer` over stdio on the vendored
  go-sdk; register `ToolRegistry` tools filtered by
  `tools.mcp_server.allow` (empty = deny-all).
- [ ] `rhizome mcp serve` subcommand — daemonless; requires
  `tools.mcp_server.enabled`; stderr-only logging.
- [ ] Config `tools.mcp_server = {enabled:false, allow:[...]}`; smoke via
  MCP inspector or a second Rhizome `tools.mcp` client.

### Track 90 — Signed curated skill index
- [ ] `pkg/skills/rhizome_registry.go` (`"rhizome"` provider): fetch
  `index.json` + `index.json.sig` → Ed25519 verify via the Track-70
  catalog-signature helper (extract shared verifier — impl-time call);
  unsigned/bad-sig → refuse.
- [ ] Entries `{slug, …, source:{github:"owner/repo[@ref][/path]"},
  sha256?}`; install delegates to the GitHub installer path (guard scan +
  provenance unchanged).
- [ ] Cache under `<RHIZOME_HOME>/skills-index-cache/` (~1 h TTL,
  stale-serve); `skill.index.merged|rejected|stale` events.
- [ ] `stpinkie/rhizome-skills` index + signing via
  `MODULE_CATALOG_SIGNING_KEY`; `docs/operations/skill-index-signing.md`;
  Hub "curated" badge.

### Track 91 — CI/DevEx automation
- [ ] `.github/labeler.yml` + `actions/labeler` — path-based PR labels.
- [ ] `pr.yml`: `concurrency` + `cancel-in-progress`; `test-parallel`
  noise review (stays advisory).

### Track 92 — Localization tail
- [ ] 9× `docs/project/README.*.md`: drop PicoClaw mascot/slogan/🦐 +
  "ultra-efficient" h1 rewording; grep-clean is the acceptance check.

### Track 93 — Docs & release v0.13.0
- [ ] `docs/guides/acp.md` (modes/config/auth/session_mode/media);
  `docs/guides/mcp-server.md`; skills guide + signing-doc links.
- [ ] `config.example.json`: ACP mode/client fields, `agents.list[].acp.*`
  additions, `tools.mcp_server`, `registries.rhizome`.
- [ ] `AGENTS.md` + `ROADMAP.md` sync; `.todo.md` rotate;
  `docs/release-notes/v0.13.0.md`; tag `v0.13.0` (explicit go-ahead).

### Acceptance criteria
- [ ] Build/test/lint gates green (`goolm,stdjson` tags; golangci-lint
  v2.13.2; `lint-docs.sh`); no new dependencies.
- [ ] v0.11.0 + v0.12.0 tags pushed, releases verified.
- [ ] Real stdio ACP drive: mid-session mode switch changes permission
  posture + survives `session/load`; model option lists `model_list` and
  switches.
- [ ] External auth: `env_var` satisfied from `acp.env` / named-missing
  error; `terminal` gated; `agent` refused clearly.
- [ ] Persistent `session_mode` reuses one session across delegations;
  image blocks reach image-capable agents.
- [ ] `rhizome mcp serve` lists/calls allowlisted tools via a real MCP
  client; deny-all default proven.
- [ ] Signed index installs end-to-end; tampered/unsigned refused;
  stale-cache serves on fetch failure.
- [ ] Zero PicoClaw/mascot hits in `docs/project/README.*.md`.
- [ ] `integration-mesh`/`integration-swarm` green.

## Open items carried

- [ ] Replace `assets/logo.webp` with actual Rhizome branding. **Blocked**:
  needs a real logo asset — no art pipeline.
- [ ] Re-enable `nightly.yml` cron + re-add `docker-build.yml` —
  **blocked** on release secrets (Docker Hub, `MACOS_SIGN_*`/
  `MACOS_NOTARY_*`).
- [ ] Phase 3 hardware: qemu 32-bit mesh tests + real-device Android smoke
  tests (deferred; no QEMU / Android test hardware; `ANDROID_API` floor is
  now 21).
- [ ] Standing watch — runtime RSS creep: re-run `measure-footprint.sh`
  each release or after large dep/runtime changes.
- [ ] Standing watch — `test-parallel` advisory CI job / `flake-hunt`: a
  recurring failure is the signal to revisit heartbeat cadence or `-p`
  throttling.

## Queued — post-v0.13.0

- [ ] **Mesh economy (deferred)**: optional-module candidate — metering +
  price sheets + receipts + bilateral ledger + settlement rails as an
  opt-in layer, not core mesh. Seed doc:
  `docs/design/mesh-economy-module.md`. Fold-in items from earlier
  "Stage 3" notes: mesh settlement and task/cost-aware smart dispatch.
  Note: remote-task usage reporting (the metering gap) is independently
  useful observability — could land standalone before any economy work.
- [ ] ACP follow-ons tail: richer `mcpCapabilities` advertisement;
  `set_config_option` categories beyond `model`; per-session model
  override (`InboundMessage` plumbing); `agent`-type authMethods;
  external-agent `session/set_mode` forwarding.
- [ ] Module trust tail: upstream cosign/GPG verification where available;
  further tenants (llama.cpp local-model runtime — needs symlink-safe
  layout-preserving extraction + a model-weights trust story; design it
  standalone rather than as a stretch).
- [ ] Node-identity store → `enc2` (Track 82 follow-up; needs a
  version-prefix scheme on `NodeIdentity`); web3 key store → `enc2`
  (same scheme question on `keys.json`).
- [ ] AIEOS: continued exploration of AI-native OS interaction paradigms.
- [ ] Android device control — **blocked** on test hardware.

## Completed (archive)

- All Phase 1 rebrand/docs, identity/security, network/CLI, build/release,
  hardware-compat, CI/CD, and v0.4 portability items.
- v0.5.0: `require_signed_caps` default true; nonce/timestamp required on
  `/rhizome/agent/1.0.0`; stale.yml cron; README news.
- v0.6.0–v0.6.2 (mesh operations): protocol unit tests; daemon/launcher
  task+audit APIs; Network Tasks/Audit panels; `active_tasks` in caps;
  `Mesh.PickPeer` + `mesh route`; `mesh-tasks.jsonl` persistence; task SSE;
  peer score store + retry/failover.
- v0.7.0 Swarm Mode (Tracks 21–30): `pkg/rhizome/swarm`, scatter-gather,
  presence, offer/claim queue, coordinator election, `swarm run`, Swarms
  panel, gossipsub transport; `integration-swarm` scripts.
- v0.7.1 (Tracks 31–32): stabilization & security (SSRF blocklists,
  prompt-injection guard, redaction, fs sandbox, mesh/swarm test
  stabilization, golangci-lint gate) + browser automation backends
  (`pkg/browser` catalog, endpoint resolvers, `tools.browser`, install
  manager, web `/browser` page, stealth skill) +
  `docs/architecture/footprint-audit.md`.
- v0.8.0 (mesh/swarm depth): blob transfer, task `--attach` attachments +
  result artifacts, media tools (`vision_mode`/`load_video`/
  `transcribe_audio`), offer cancel/retry/dead-letter + capability-aware
  claims, orchestration v2 (DAG waves, run records, dashboard run panel),
  `/rhizome/pair/1.0.0` trust pairing, mesh skill distribution, MCP
  context7 preset, `pkg/browser/cdp` native CDP client.
- v0.8.1–v0.8.5: release-readiness hardening; small-VM validation path
  (`validate-small-vm.sh`, `lint-slim`, `build-core`); swarm re-announce
  roster healing; `Swarm.save` serialization; `ReliableConn.ReadFrame`
  drain fix; stderr→stdout swarm CLI fix; `SkillShare` race fix; pnpm
  format gate.
- v0.9.0 (Tracks 54–59): upstream defect burn-down (config multi-key
  collapse, sensitiveCache race, Telegram animator, QQ 401, session
  archive, web chat lag, channel flat-key compat); attachment matrix;
  onboarding wizard + web `/setup`; swarm blackboard; signed agent
  manifests; `session_header`, `openai-compatible` preset, IRC splitting.
- v0.9.1: `Syncer.Stop` WaitGroup panic fix; swarm claim-window drain;
  test-timing race fixes; lint sweep; small-VM OOM caps; **first tag with
  real release artifacts**.
- Track A (release plumbing): `release.yml` on `push: v*`; `create-tag.yml`
  dispatch; `nightly.yml` Docker-Hub-optional; `goreleaser check` in
  pr.yml; `release-smoke.yml` weekly; winres snapshot fix; smoke run caught
  the netbsd `Bsize` blocker.
- Track B (parallel test flakes): mDNS/NAT isolation + `testutil.NewIdentity`;
  deterministic swarm queue tests; `scripts/flake-hunt.{sh,ps1}` + advisory
  `test-parallel` job; `TestMDNSDiscoversPeer`.
- Track C (footprint philosophy, partial — tails moved to Track 64):
  "ultra-lightweight"→ubiquity re-spec; doc sweep (README + 9 locales +
  CONTRIBUTING + workspace docs); binary-size gate in `build.yml`.
- v0.10.0 (Tracks 60–66): companion-module system (`pkg/modules`, `rhizome
  module`, `/modules` page); ACP server (`rhizome acp`) + ACP client
  (`agents.list[].acp`, `acp_run`); mesh observability (conns/score/
  bandwidth/activity feed, `mesh peer`/`mesh activity`, dashboard panels);
  worker-node tier + `measure-footprint.sh` (worker ≈50 MB / full ≈55 MB);
  `nimbus-verified-proxy` + `ethereum-rpc` first tenants (P2P LC default);
  ROADMAP re-eval + guides + release notes.
- v0.11.0 (Tracks 67–68, 70–71 — merged scope): shipped web3 Stage 2 reads
  (`pkg/web3` client/resolver, 7 read-only `web3_*` tools, SSRF posture,
  30 s endpoint cache); module trust (Ed25519-signed catalog + opt-in
  remote index `module_index` + `module verify` drift detection + secret-
  gated release signing); new tenants (`helios` linux/darwin, `ipfs-kubo`
  all platforms; `.zip` extraction, per-GOOS `AssetTemplates`,
  `{module_dir}` placeholder, `init_marker`/`init_args`/`setup_args`,
  catalog schema v2). Open Tracks 69/72/73/74/75 moved to v0.12.0 as
  Tracks 77/81/80/82 (75 absorbed into 76+83).
