# Phase 1 Leftovers

These items are not blockers, but they should be cleaned up before Phase 1 is considered fully polished.

## Rebrand / Docs
- [x] Replace remaining `picoclaw.io` links with a new docs domain or GitHub repo links.
  - [x] `README.md` (badges, download links, official domain text)
  - [x] `docs/project/README.*.md` (all translations)
  - [x] `docs/guides/android-termux.md`
  - [x] `web/backend/i18n.go` (`DocUrl` English + Chinese)
  - [x] `web/frontend/src/components/app-header.tsx`
  - [x] `web/frontend/src/components/channels/channel-config-page.tsx`
- [x] Rewrite `README.md` top section: state Rhizome is a hard fork of PicoClaw.
- [x] Verify / update `LICENSE` and `CONTRIBUTING.md` to keep PicoClaw attribution while adding Rhizome copyright.
- [ ] Replace `assets/logo.webp` and ASCII banner in `cmd/rhizome/main.go` with actual Rhizome branding (deferred to Phase 2+).
- [ ] Replace PicoClaw mascot slogan/shrimp emoji in translated READMEs (deferred to Phase 2+).

## Identity / Security
- [x] Encrypt the persisted node identity (`~/.rhizome/identity/node.json`) with a local passphrase.
- [x] Support optional BIP39 mnemonic generation in `rhizome network onboard`.
- [x] Decide whether to cache the BIP39 root seed (encrypted) for network auth, or require the user to re-enter the mnemonic when needed.

## Network / CLI
- [x] `rhizome network onboard` should validate that the chosen `node_index` is not already in use on the local machine.
- [x] `rhizome daemon` currently only starts the P2P host; it should eventually start the agent gateway as well (Phase 2+).
- [x] Add DHT discovery as a fallback when mDNS is not sufficient.
- [x] Add integration test that runs `rhizome daemon` and `rhizome network ping` as real processes.

## Build / Release
- [x] Review `.goreleaser.yaml` Docker image names and Docker Hub env vars.
- [x] Confirm `web/rhizome-launcher.png` and desktop file references are wired up in the frontend build.
- [x] Measure binary size impact of libp2p; decide whether to add a `nonetwork` build tag for lightweight builds.
- [x] Add a `nonetwork` build tag to compile out libp2p/DHT/mesh and reduce the binary size for very small boards (deferred to Phase 2+). (Removed — does not differentiate from PicoClaw.)

## Hardware Compatibility
- [x] Review the hardware-compatibility list and remove devices that cannot run the current ~98 MB binary.
- [x] Update the README and docs with the two-tier (Base/Full) resource requirements.

## CI/CD
- [x] Remove `upload-tos.yml` and the `upload-tos` job from `release.yml` (Volcengine TOS is not used).
- [x] Remove `docker-build.yml` (redundant with GoReleaser; can be restored from git history when Docker Hub credentials are configured).
- [x] Update CI to cross-compile `linux/386`, `windows/386`, `linux/arm`, `android/arm64` (PR and build.yml) and produce the Android universal bundle (build.yml, release.yml, nightly.yml).
- [x] Install Android NDK via `nttld/setup-ndk` in build/release/nightly workflows so cgo Android targets (`android/arm`, `android/386`, `android/amd64`) can build.
- [ ] Re-enable scheduled `nightly.yml` and `stale.yml` workflows when release infrastructure (Docker Hub, GHCR, macOS notary) and issue-management policies are ready.
- [ ] Re-add `docker-build.yml` if a separate manual Docker build is needed once Docker Hub credentials are in place.

## v0.4 Portability (Phase 4)
- [x] Add `linux/386`, `windows/386`, and Android cgo targets to `Makefile` and `.goreleaser.yaml`.
- [x] Harden P2P transport in `pkg/rhizome/network/host.go` (TCP-only fallback, best-effort mDNS).
- [x] Matrix enabled on Android arm64 (removed `!android` build tag from `pkg/gateway/channel_matrix.go`).
- [x] Update hardware compatibility docs, README, and portability matrix.
- [ ] Phase 3 — qemu 32-bit mesh tests and real-device Android smoke tests (deferred; current environment lacks QEMU / Android 4.4 test hardware).
- [ ] Phase 5 — final README/translation refresh and v0.4.0 release notes.
