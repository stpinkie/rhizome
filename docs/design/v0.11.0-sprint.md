# v0.11.0 Sprint Plan — Agentic Operations & Trust

Sprint tracks and acceptance criteria live in `.todo.md`; this document is the
durable design record: ordering, per-track architecture, and the risk register.

Theme: the mesh grows **teeth and trust**. Web3 Stage 2 turns the
nimbus-verified-proxy endpoint into agent tools — reads first, then gated
signing with a human-approval queue. The module system gains a signed catalog
and curated index so tenants can ship out-of-band. ACP deepens (session/load,
MCP passthrough, terminal capability), the mesh becomes role-aware, and the
dashboard gets a topology view. First, we actually ship v0.10.0.

## Ordering & dependencies

One PR per track, in this order:

```
Track 67  Ship v0.10.0 ──────────────── first (clears the deck; ops, not code)
Track 70  Module trust (signed catalog + index) ──┐
Track 71  New tenants (helios, kubo) ──────────────┘ (71 rides 70's machinery)
Track 68  Web3 reads (pkg/web3 + web3_* tools) ────┐
Track 69  Web3 wallet + gated signing ─────────────┘ (69 depends on 68)
Track 72  ACP deepening (independent)
Track 73  Mesh depth + topology graph (independent)
Track 74  Security polish — stretch, first cut line
Track 75  Docs & release (last)
```

Rationale: the release gate clears before new work so `.todo.md` stays honest;
module trust lands before new tenants so they ride the signed-catalog path;
web3 signing builds on the read client; everything else is independent and
flexes around the big tracks.

**Sprint is deliberately large** (9 tracks vs v0.10.0's 7). Cut lines, in
order: Track 74 → the topology-graph half of 73 → the signing half of 69
(ship reads-only). Record cuts in `.todo.md` like previous sprints.

## Decisions

- **No `go-ethereum`.** Web3 needs secp256k1 + Keccak-256 + RLP + a minimal
  ABI codec — `decred/dcrd/dcrec/secp256k1/v4` and `golang.org/x/crypto` are
  already in the module graph (indirect deps); go-ethereum would blow the
  +15% binary-size warn gate. Record the delta per track.
- **Signing approval is async.** No built-in human approver exists outside
  ACP (`ToolApprover`/`approve_tool` is the generic seam —
  `pkg/agent/hooks.go`, `docs/architecture/hooks/hook-json-protocol.md`).
  The mechanism is a **pending-approval queue**: the tool returns a
  `pending_id`, the human resolves via `rhizome web3 approve <id>` or
  `POST /web3/approvals/<id>` (daemon + launcher + dashboard badge), and
  broadcast happens on approval. Works on every channel without
  channel-specific UX.
- **Module trust root = a Rhizome Ed25519 release key** baked into the
  binary. `catalog.json` + signature (Ed25519 over canonical JSON;
  minisign-format compatibility optional — `aead.dev/minisign` is already
  in the graph). The embedded catalog remains the default; a remote curated
  index is opt-in (`modules.catalog_url`) and unsigned/bad-sig catalogs are
  refused hard.
- **Topology graph is hand-rolled SVG** — `web/frontend/package.json` has no
  graph lib and we keep it that way (bundle-lean; radial layout: self
  center, peers ringed, edges colored by transport).
- **`enc2://` = XChaCha20-Poly1305**, transparent dual-decrypt with
  `enc://` (AES-256-GCM stays readable forever); new writes use enc2.
  `x/crypto/chacha20poly1305` is already in the graph.

## Track 67 — Ship v0.10.0

**As executed:** planning surfaced that v0.10.0 had in fact already been
published (tag `3dd48db4` → `6dae884f`, release 2026-09-15) — *before* PR
#13 (`nimbus p2p` default) merged, so the published artifacts lacked a
change the release notes describe. Per owner decision the release was
re-cut rather than patched: `gh release delete v0.10.0 --cleanup-tag` →
re-tag `v0.10.0` at `8515dbe1` (preserving the annotated message) → push →
`release.yml` re-runs on the push event. The canonical
`create-tag.yml` path refuses pre-existing tags, which is why the delete
came first.

- Linux nimbus smoke (the last unchecked v0.10.0 box): verified on
  linux/amd64 under WSL2 with a `main` build — install (sha512-verified
  v0.4.1) → configure (`execution_api_url` secret + fetched
  `trusted_block_root`, `p2p` default) → daemon-supervised start →
  `LC bootstrap verified`, headers following tip, `eth_syncing=false`,
  `eth_chainId=0x1`, `eth_blockNumber` verified → supervised `stop`.
- Verify: all archives on the release page, `docker pull
  ghcr.io/stpinkie/rhizome:v0.10.0`, `rhizome version` in an artifact.
- `.todo.md`: check the two open boxes; fold v0.10.0 into the archive.

## Track 68 — Web3 reads

- **`pkg/web3`** (new package — eth specifics stay out of `pkg/tools`):
  - `client.go` — JSON-RPC 2.0 client on stdlib `net/http` (no dep);
    `Call(ctx, method, params)`; timeout from `tools.web3.timeout_seconds`;
    optional Bearer header (`ethereum-rpc.api_key`).
  - `endpoint.go` — `EndpointResolver` precedence: `tools.web3.endpoint`
    override → configured `modules.ethereum-rpc` `endpoint_url`
    (`Manager.resolvedFields` incl. secret api_key) → **running+healthy**
    `nimbus-verified-proxy` `listen_url` → descriptive error. Endpoints are
    config-derived only — never agent-supplied at call time; http/https
    only (SSRF posture consistent with `pkg/guard`).
  - `types.go` — hex quantity codec, block/tx/receipt/log types.
- **Tools** under `pkg/tools/web3/` (facade like `pkg/tools/browser`):
  `web3_call` (eth_call), `web3_balance`, `web3_block`, `web3_transaction`
  (receipt+status), `web3_logs` (range-capped), `web3_chain` (chainId, gas,
  syncing), and `web3_rpc` — generic passthrough **restricted to a
  read-only method allowlist** (`eth_call`/`eth_get*`/`net_*`/
  `web3_clientVersion`; `*_send*`/`eth_sign*` excluded).
- Config `tools.web3 = {enabled:false, endpoint:"", chain_ids:[],
  max_log_range:10000, timeout_seconds:30}` — **disabled by default**;
  `chain_ids` constrains reachable networks. Registered per
  `cfg.Tools.IsToolEnabled` in `pkg/agent/agent_init.go`; `api_key` joins
  the sensitive-data replacer.

## Track 69 — Web3 wallet + gated signing

- `pkg/web3/keys.go` — secp256k1 keys (`dcrec/secp256k1`), Keccak-256
  addresses (`x/crypto/sha3`). Store `<RHIZOME_HOME>/web3/keys.json` (dir
  `0700`) encrypted via the `pkg/rhizome/identity` KeyProvider posture
  (keyring default, passphrase fallback); import/generate; `--reveal`
  export gated + masked.
- `pkg/web3/abi.go` — minimal ABI codec (address/uint/int/bool/bytes/
  string/bytesN + dynamic types for ERC-20/721); table-tested against spec
  vectors.
- `pkg/web3/tx.go` — EIP-1559 + legacy build/sign (RLP),
  `eth_estimateGas`, nonce via `eth_getTransactionCount`,
  `eth_sendRawTransaction` broadcast. Keys never leave the box.
- `pkg/web3/policy.go` — `tools.web3.signing = {enabled:false,
  allow_contracts:[], allow_methods:["0x…selector"], max_value_wei_per_tx,
  max_value_wei_per_day, chain_ids:[]}`; persistent spend ledger
  `<RHIZOME_HOME>/web3-ledger.jsonl` (bounded, `mesh-tasks.jsonl` pattern);
  violations deny with reason.
- **Approval queue** — `<RHIZOME_HOME>/web3-pending.json` (bounded);
  `web3_send`/`web3_sign` pass policy → enqueue → return
  `{pending_id, summary}` → human approves via `rhizome web3 approve|reject
  <id>` or `POST /web3/approvals/<id>` (+ launcher `/api/web3/*` proxy +
  dashboard badge) → sign+broadcast. External `hook.approve_tool` hooks
  still get first pass via `HookManager.ApproveTool`; timeout/cancel →
  deny. `web3_pending`/`web3_send_status` tools let the agent poll.
- Tools: `web3_send`, `web3_sign` (EIP-191), `web3_approve` (ERC-20),
  `web3_wallet` (addresses+balances; never key material). CLI:
  `rhizome wallet create|import|list|reveal`, `rhizome web3
  pending|approve|reject`. Events `web3.pending|approved|rejected|sent` →
  activity feed + audit.
- **Canary posture:** `signing.enabled` defaults false; testnet-first docs.

## Track 70 — Module trust

- `pkg/modules/catalog.go` — embed the release pubkey; `CatalogSignature`
  verify helper; `catalog.json` produced at release time and signed by the
  Rhizome Ed25519 release key (signature covers every entry incl. digest
  pins → trust root is the key, not the network).
- Remote index: `modules.catalog_url` (default empty = embedded only).
  Fetch `catalog.json`+`.sig` over HTTPS → verify → cache under
  `<RHIZOME_HOME>/catalog-cache/` → `module list` merges embedded+remote
  with a `source` provenance flag; unsigned/bad-sig → refuse + warn event.
- `rhizome module verify <id>` — re-hash the installed binary against its
  pin (drift detection).
- Stretch inside track: upstream cosign/GPG verification where upstreams
  actually sign (verification item; most candidates don't — deferrable).

## Track 71 — New module tenants

- `helios` (`a16z/helios`): linux/darwin catalog entry — complements nimbus
  (nimbus ships Windows, helios doesn't; helios covers darwin/amd64).
  Asset naming + checksum shape = verification item.
- `ipfs-kubo` (`ipfs/kubo`): daemon kind; requires **zip archive support**
  in `pkg/modules/download.go` (tar.gz-only today — `extractTarGz` +
  `archive/zip` sibling w/ the same traversal + decompression caps);
  `repo`/data-dir + ports as config fields.
- Stretch tenant: `llama.cpp` `llama-server` (ondemand) — upstream asset
  naming is churny; allowed to slip.
- Each tenant: catalog entry + `module validate` coverage + a real-download
  smoke on ≥1 platform.

## Track 72 — ACP deepening

- **`session/load`:** advertise `loadSession:true`; implement the SDK's
  `AgentLoader` (`LoadSession(ctx, LoadSessionRequest)` — confirmed at
  `agent_gen.go:381`, dispatched only when the agent implements it).
  Persist `acpSessionId → agent:<id>:acp:<sid>` at
  `<RHIZOME_HOME>/acp-sessions.json`; on load, replay stored session
  history (workspace `sessions/*.jsonl`) as `session/update` chunks;
  optionally restore cached `*_always` permission decisions.
- **MCP passthrough:** honor `session/new.mcpServers` — per-session
  connections via `pkg/mcp` (`Manager.ConnectServer` exists); tools surface
  namespaced (`mcp__<server>__<tool>`) in the turn registry; teardown on
  close/cancel; bounded server count + declared-`env`-only hygiene.
- **Terminal client capability:** serve `terminal/*` from external ACP
  agents through the guarded shell path (`pkg/tools` shell security +
  `pkg/isolation` where available); new `acp.client.terminal_policy` =
  `deny` (default) | `allow`; bounded output. High-risk → default deny.
- Keep stdout protocol-clean (`RHIZOME_LOG_FILE`/stderr discipline) in both
  directions.

## Track 73 — Mesh depth

- **Role-aware `PickPeer`** (`mesh.rankedCandidates`): `Capability.Role`
  joins the composite score behind `mesh.routing.role_aware` (default
  true). Semantics: workers stay preferred *executors* for task ops;
  infra-dependent ops (relay/DHT-adjacent) prefer `full`. Concrete weights
  decided at impl time and documented.
- **Coordinator preference** (`swarm.reelectLocked`, currently lexicographic
  min peer id): extend `MemberInfo`/presence with `Role` sourced from the
  capability manifest store (additive field — older peers must keep
  parsing); election order = `full` members first, then workers, peer-id
  tiebreak (deterministic, all members compute the same winner).
- **Topology graph:** `web/frontend/src/components/network/
  topology-panel.tsx` — hand-rolled SVG radial layout fed by
  `/api/network/status` `peers[].conns` (transport/direction already
  exposed); click-through to the existing peer detail Sheet. No new JS dep.
- Smart routing stays bounded to role + requirements matching; cost/token
  economics deferred (ties into Web3 Stage 3 mesh settlement).

## Track 74 — Security polish (stretch)

- `enc2://` in `pkg/credential`: XChaCha20-Poly1305, same HKDF shape,
  `salt|nonce|ct` blob; transparent decrypt of `enc://` + `enc2://`;
  `Encrypt` writes `enc2:`; documented re-encrypt path. Node-identity store
  stays AES-GCM this sprint (no scheme prefix there — follow-up).
- OAuth residual: verify what `LoginBrowserOptions.NoBrowser` already
  covers (manual paste?) → fill only the actual gap (device-code grant
  where supported, or a documented headless flow) + `providers.md` updates.

## Track 75 — Docs & release

- `docs/guides/web3.md` (tools, wallet, policy, approval flow,
  testnet-first), `docs/guides/modules.md` (trust/catalog_url/tenants),
  `docs/guides/acp.md` (session/load, MCP passthrough, terminal policy),
  routing docs update.
- `config.example.json`: `tools.web3`, `modules.catalog_url`,
  `acp.client.terminal_policy`, `mesh.routing` sections.
- `AGENTS.md` sync; `ROADMAP.md` sync **incl. stale-item corrections**
  (OneBot + `find_skills` shipped; OAuth partially shipped — see below);
  `.todo.md` archive rotate.
- `docs/release-notes/v0.11.0.md`; tag `v0.11.0` + push.

## Stale roadmap corrections (baked into Track 75)

Research during sprint planning found three roadmap items already shipped:

- **OneBot** — `pkg/channels/onebot` (registered factory, config example,
  `docs/channels/onebot/`).
- **`find_skills`/`install_skill`** — `pkg/tools/integration/skills_*.go`
  + `pkg/skills` ClawHub/GitHub registries + Hub marketplace UI; enabled in
  `config.example.json`.
- **OAuth 2.0** — `pkg/auth/oauth.go` browser+PKCE flow,
  `pkg/providers/oauth/` (codex/claude/antigravity), `auth_method`
  plumbing. Residual = headless/device path → Track 74.

## Acceptance criteria

- `go build -tags goolm,stdjson ./...` green; `go test -p 1 -tags
  goolm,stdjson ./...` green on touched packages; `golangci-lint` v2.13.2 →
  0; `lint-docs.sh` OK; binary-size delta recorded per dep-adding track.
- v0.10.0 published: all assets + GHCR pull verified; Linux nimbus smoke
  green.
- Web3 reads verified against nimbus proxy (sepolia) + `ethereum-rpc`;
  signing verified end-to-end on testnet incl. policy denies, cap
  enforcement, pending→approve→broadcast, deny-on-timeout.
- `module verify` + signed remote catalog verified (bad-sig refuse path
  tested); helios + kubo install/start/status/stop smoke on ≥1 platform.
- ACP: `session/load` round-trips a prior session in a real stdio drive;
  mcpServers spawn + tools visible; terminal deny-by-default proven.
- Mesh: role-aware ranking + coordinator preference unit-tested; topology
  panel builds clean from live `/api/network/status`.
- `integration-mesh`/`integration-swarm` green; networked tests follow the
  AGENTS.md isolation rules.

## Impl-time verification checklist

- `mcpServers` schema shape in the pinned SDK (v0.13.5) + `session/load`
  replay semantics (which update kinds clients expect).
- helios + kubo release asset names/platforms/checksums; whether kubo ships
  `.zip` for windows (drives zip support in `download.go`).
- `pkg/mcp` per-session connect/teardown under concurrent sessions (Close
  semantics, tool-name collisions).
- What `LoginBrowserOptions.NoBrowser` already covers → the real OAuth gap.
- ERC-20/ABI edge vectors; EIP-1559 fields vs the nimbus proxy's supported
  tx types.
- Presence/manifest path for `Role` reaching swarm `MemberInfo` — additive
  only, older peers must keep parsing.
- Whether pushing `v0.10.0` needs `create-tag.yml` dispatch instead of a
  raw tag push (both paths documented in AGENTS.md — raw push fires
  `release.yml` directly).

## Risk register

| Risk | Mitigation |
| --- | --- |
| Signing ships real-money risk | `signing.enabled` default false; allowlist+caps+pending-approval mandatory; audit ledger; testnet-first docs |
| No built-in human approver outside ACP | Pending-approval queue (CLI + daemon API + UI badge) — Track 69's key mechanism |
| Heavy web3 deps | go-ethereum excluded; secp256k1 + sha3 + minimal RLP/ABI only (already in module graph) |
| Remote catalog = supply-chain surface | Signature mandatory, embedded catalog is trust root, unsigned remote refused |
| ACP terminal capability is dangerous | `terminal_policy` default deny; routed through existing shell guards |
| Sprint breadth (9 tracks) | Cut lines declared up front: 74 → topology half of 73 → signing half of 69 |
| Tag push fires a real 2–3 h release | Track 67 runs only with explicit go-ahead |
