version: "2"

# .golangci.yaml
#
# errcheck, gosec, and staticcheck are intentionally enabled. The
# `exclusions.rules` path exception below only de-scopes the historical lint
# debt in cmd/, web/, examples/, scripts/, and non-stabilization pkg/ paths.
# The v0.8.2 stabilization packages (pkg/rhizome, pkg/media, pkg/tools/fs) are
# fully linted by these linters.

linters:
  default: all
  disable:
    # TODO: Tweak for current project needs
    - containedctx
    - cyclop
    - depguard
    - dupword
    - err113
    - exhaustruct
    - exhaustruct_v5
    - funcorder
    - gomodguard
    - gochecknoglobals
    - gosmopolitan  # Project legitimately uses CJK text in tests (FTS5, token counting)
    - godot
    - intrange
    - ireturn
    - nlreturn
    - noctx
    - noinlineerr
    - nonamedreturns
    - recvcheck
    - tagliatelle
    - testpackage
    - varnamelen
    - wrapcheck
    - wsl
    - wsl_v5

    # TODO: Disabled, because they are failing at the moment, we should fix them and enable (step by step)
    - contextcheck
    - embeddedstructfieldcheck
    - errchkjson
    - errorlint
    - exhaustive
    - forbidigo
    - forcetypeassert
    - funlen
    - gochecknoinits
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godox
    - ineffassign
    - lll
    - maintidx
    - mnd
    - misspell
    - modernize
    - nestif
    - nilnil
    - paralleltest
    - perfsprint
    - revive
    - tagalign
    - testifylint
    - thelper
    - unparam
    - usestdlibvars
    - usetesting
  settings:
    gomoddirectives:
      replace-allow-list:
        - github.com/bwmarrin/discordgo
    errcheck:
      check-type-assertions: true
      check-blank: false
    exhaustive:
      default-signifies-exhaustive: true
    funlen:
      lines: 120
      statements: 40
    gocognit:
      min-complexity: 25
    gocyclo:
      min-complexity: 20
    govet:
      enable-all: true
      disable:
        - fieldalignment
        - shadow
        - inline
    lll:
      line-length: 120
      tab-width: 4
    misspell:
      locale: US
    mnd:
      checks:
        - argument
        - assign
        - case
        - condition
        - operation
        - return
    nakedret:
      max-func-lines: 3
    revive:
      enable-all-rules: true
      rules:
        - name: add-constant
          disabled: true
        - name: argument-limit
          arguments:
            - 7
          severity: warning
        - name: banned-characters
          disabled: true
        - name: cognitive-complexity
          disabled: true
        - name: comment-spacings
          arguments:
            - nolint
          severity: warning
        - name: cyclomatic
          disabled: true
        - name: file-header
          disabled: true
        - name: function-result-limit
          arguments:
            - 3
          severity: warning
        - name: function-length
          disabled: true
        - name: line-length-limit
          disabled: true
        - name: max-public-structs
          disabled: true
        - name: modifies-value-receiver
          disabled: true
        - name: package-comments
          disabled: true
        - name: unused-receiver
          disabled: true
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        source: '^//go:generate '
      - linters:
          - funlen
          - maintidx
          - gocognit
          - gocyclo
        path: _test\.go$
      - linters:
          - nolintlint
        path: 'pkg/tools/(i2c\.go|spi\.go)$'
      # v0.8.2 stabilization scope: the re-enabled errcheck/staticcheck/gosec
      # are enforced in pkg/rhizome, pkg/media, and pkg/tools/fs. The rest of
      # the repository still carries legacy lint debt and is deferred to
      # post-v0.8.2 cleanup sprints; the production code in the stabilization
      # scope is still linted.
      - linters:
          - errcheck
          - gosec
          - staticcheck
          - nolintlint
          - whitespace
        path: '^(cmd|web|examples|scripts)(?:/|\\\\)|^pkg/(?:agent|audio|auth|browser|bus|channels|commands|config|constants|credential|cron|devices|events|evolution|fileutil|gateway|guard|health|heartbeat|identity|isolation|logger|mcp|memory|migrate|netbind|pid|providers|redact|routing|seahorse|session|skills|state|tokenizer|updater|utils)(?:/|\\\\)|^pkg/tools/(?:[^f]|f[^s]|fs[^/\\\\])'
      # Tests intentionally ignore Close/Stop errors in t.Cleanup and use
      # permissive permissions in fixtures; the production code is still linted.
      - linters:
          - errcheck
          - gosec
        path: _test\.go$

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
  settings:
    gci:
      sections:
        - standard
        - default
        - localmodule
      custom-order: true
    gofmt:
      simplify: true
      rewrite-rules:
        - pattern: "interface{}"
          replacement: "any"
        - pattern: "a[b:len(a)]"
          replacement: "a[b:]"
    golines:
      max-len: 120
