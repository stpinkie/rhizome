# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2

git:
  ignore_tags:
    - nightly
    - ".*-nightly.*"

before:
  hooks:
    - go generate ./...
    - sh -c 'cd web/frontend && CI=true pnpm install --frozen-lockfile && pnpm build:backend'
    - sh -c 'GOBIN="$(go env GOPATH)/bin"; mkdir -p "$GOBIN"; go install github.com/tc-hib/go-winres@v0.3.3 && "$GOBIN/go-winres" make --in web/backend/winres/winres.json --out web/backend/rsrc --product-version={{ .Version }} --file-version={{ .Version }}'
    - sh -c 'if [ "${INCLUDE_ANDROID_BUNDLE:-}" = "true" ]; then make build-android-bundle; fi'

builds:
  - id: rhizome
    env:
      - CGO_ENABLED=0
    tags:
      - goolm
      - stdjson
    ldflags:
      - -s -w
      - -X github.com/stpinkie/rhizome/pkg/config.Version={{ .Version }}
      - -X github.com/stpinkie/rhizome/pkg/config.GitCommit={{ .ShortCommit }}
      - -X github.com/stpinkie/rhizome/pkg/config.BuildTime={{ .Date }}
      - -X github.com/stpinkie/rhizome/pkg/config.GoVersion={{ with index .Env "GOVERSION" }}{{ . }}{{ else }}unknown{{ end }}
    goos:
      - linux
      - windows
      - darwin
      - freebsd
      - netbsd
    goarch:
      - amd64
      - 386
      - arm64
      - riscv64
      - loong64
      - arm
      - s390x
      - mipsle
    goarm:
      - "6"
      - "7"
    gomips:
      - softfloat
    main: ./cmd/rhizome
    ignore:
      - goos: windows
        goarch: arm
      - goos: netbsd
        goarch: arm

  - id: rhizome-launcher
    binary: rhizome-launcher
    env:
      - CGO_ENABLED=0
    tags:
      - goolm
      - stdjson
    ldflags:
      - -s -w
      - -X github.com/stpinkie/rhizome/pkg/config.Version={{ .Version }}
      - -X github.com/stpinkie/rhizome/pkg/config.GitCommit={{ .ShortCommit }}
      - -X github.com/stpinkie/rhizome/pkg/config.BuildTime={{ .Date }}
      - -X github.com/stpinkie/rhizome/pkg/config.GoVersion={{ with index .Env "GOVERSION" }}{{ . }}{{ else }}unknown{{ end }}
    goos:
      - linux
      - windows
      - darwin
      - freebsd
      - netbsd
    goarch:
      - amd64
      - 386
      - arm64
      - riscv64
      - loong64
      - arm
      - s390x
      - mipsle
    goarm:
      - "6"
      - "7"
    gomips:
      - softfloat
    main: ./web/backend
    ignore:
      - goos: windows
        goarch: arm
      - goos: netbsd
        goarch: arm

  - id: rhizome-android
    binary: rhizome
    env:
      - CGO_ENABLED=0
    tags:
      - goolm
      - stdjson
    ldflags:
      - -s -w
      - -checklinkname=0
      - -X github.com/stpinkie/rhizome/pkg/config.Version={{ .Version }}
      - -X github.com/stpinkie/rhizome/pkg/config.GitCommit={{ .ShortCommit }}
      - -X github.com/stpinkie/rhizome/pkg/config.BuildTime={{ .Date }}
      - -X github.com/stpinkie/rhizome/pkg/config.GoVersion={{ with index .Env "GOVERSION" }}{{ . }}{{ else }}unknown{{ end }}
    goos:
      - android
    goarch:
      - arm64
    main: ./cmd/rhizome

# Docker images default to stpinkie/rhizome on Docker Hub.
# Override DOCKERHUB_IMAGE_NAME to publish from a fork.
dockers_v2:
  - id: rhizome
    dockerfile: docker/Dockerfile.goreleaser
    extra_files:
      - docker/entrypoint.sh
    ids:
      - rhizome
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/rhizome"
      - 'docker.io/{{ if isEnvSet "DOCKERHUB_IMAGE_NAME" }}{{ .Env.DOCKERHUB_IMAGE_NAME }}{{ else }}stpinkie/rhizome{{ end }}'
    tags:
      - '{{ if isEnvSet "NIGHTLY_BUILD" }}nightly{{ else }}{{ .Tag }}{{ end }}'
      - '{{ if isEnvSet "NIGHTLY_BUILD" }}nightly{{ else }}latest{{ end }}'
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/riscv64

  - id: rhizome-launcher
    dockerfile: docker/Dockerfile.goreleaser.launcher
    ids:
      - rhizome
      - rhizome-launcher
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/rhizome"
      - 'docker.io/{{ if isEnvSet "DOCKERHUB_IMAGE_NAME" }}{{ .Env.DOCKERHUB_IMAGE_NAME }}{{ else }}stpinkie/rhizome{{ end }}'
    tags:
      - '{{ if isEnvSet "NIGHTLY_BUILD" }}nightly-launcher{{ else }}{{ .Tag }}-launcher{{ end }}'
      - '{{ if isEnvSet "NIGHTLY_BUILD" }}nightly-launcher{{ else }}launcher{{ end }}'
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/riscv64

notarize:
  macos:
    - enabled: '{{ isEnvSet "MACOS_SIGN_P12" }}'
      ids:
        - rhizome
        - rhizome-launcher
      sign:
        certificate: "{{.Env.MACOS_SIGN_P12}}"
        password: "{{.Env.MACOS_SIGN_PASSWORD}}"
      notarize:
        issuer_id: "{{.Env.MACOS_NOTARY_ISSUER_ID}}"
        key_id: "{{.Env.MACOS_NOTARY_KEY_ID}}"
        key: "{{.Env.MACOS_NOTARY_KEY}}"
        wait: true
        timeout: 20m

archives:
  - formats: [tar.gz]
    ids:
      - rhizome
      - rhizome-launcher
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]

  # The android build produces a single binary while desktop archives carry
  # both rhizome and rhizome-launcher; keep it in its own archive so binary
  # counts are consistent per archive (GoReleaser v2 validation).
  - formats: [tar.gz]
    ids:
      - rhizome-android
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

nfpms:
  - id: rhizome
    ids:
      - rhizome
      - rhizome-launcher
    package_name: rhizome
    file_name_template: >-
      {{ .PackageName }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else if eq .Arch "arm" }}armv{{ .Arm }}
      {{- else }}{{ .Arch }}{{ end }}
    vendor: rhizome
    homepage: https://github.com/{{ .Env.GITHUB_REPOSITORY_OWNER }}/rhizome
    maintainer: rhizome contributors
    description: rhizome - a tool for managing and running tasks
    license: MIT
    formats:
      - rpm
      - deb
    bindir: /usr/bin
    contents:
      - src: web/rhizome-launcher.desktop
        dst: /usr/share/applications/rhizome-launcher.desktop
      - src: web/rhizome-launcher.png
        dst: /usr/share/icons/hicolor/512x512/apps/rhizome-launcher.png

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# upx:
#    - enabled: true
#      compress: best
#      lzma: true

release:
  disable: '{{ isEnvSet "NIGHTLY_BUILD" }}'
  extra_files:
    - glob: ./build/rhizome-android-universal.zip
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
