services:
  integration-runner:
    image: golang:1.25-bookworm
    working_dir: /workspace
    entrypoint: ["bash", "-c"]
    volumes:
      - ${INTEGRATION_REPO_ROOT}:/workspace
      - rhizome-integration-gocache:/go-build-cache
      - rhizome-integration-gomodcache:/go-mod-cache
    environment:
      GOCACHE: /go-build-cache
      GOMODCACHE: /go-mod-cache
      GOTOOLCHAIN: local
      CGO_ENABLED: "0"
      GOFLAGS: -tags=goolm,stdjson,integration

volumes:
  rhizome-integration-gocache:
  rhizome-integration-gomodcache:
